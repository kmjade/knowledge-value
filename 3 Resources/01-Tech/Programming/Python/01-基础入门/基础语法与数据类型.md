---
title: 基础语法与数据类型
status: active
priority: high
tags: [resource/tech/programming/python, python/basics, python/beginner]
aliases: [Python基础语法, Python数据结构]
created: 2026-02-01
---

# 基础语法与数据类型

> 🎯 **学习目标**：掌握Python的基础语法体系和核心数据类型，为后续学习打下坚实基础。

## 📑 Python语法概览

### 语言特点
- **解释型语言**：无需编译，直接运行
- **动态类型**：变量类型在运行时确定
- **强类型语言**：不支持隐式类型转换
- **简洁优雅**：语法简洁，代码可读性强

### 基本语法规则

#### 代码结构
```python
# Python使用缩进来表示代码块
# 标准缩进为4个空格

def greet(name):
    if name:
        print(f"Hello, {name}!")
    else:
        print("Hello, World!")

# 调用函数
greet("Python")  # Hello, Python!
greet("")       # Hello, World!
```

#### 注释规范
```python
# 单行注释：使用#符号

"""
多行注释：
使用三重引号
可以跨越多行
"""

# 文档字符串（docstring）
def function_name():
    """
    函数功能描述
    
    Args:
        参数说明
    
    Returns:
        返回值说明
    
    Example:
        >>> function_name()
        'result'
    """
    pass
```

## 🔤 基础数据类型

### 数字类型（ Numbers）

#### 整数（ int ）
```python
# 不同进制的表示
decimal_number = 123        # 十进制
binary_number = 0b1010      # 二进制 (10)
octal_number = 0o12         # 八进制 (10)
hexadecimal_number = 0xA    # 十六进制 (10)

# 大整数支持
big_number = 123456789012345678901234567890

# 整数运算
print(10 + 5)     # 15 (加法)
print(10 - 3)     # 7  (减法)  
print(10 * 6)     # 60 (乘法)
print(10 / 3)     # 3.333... (除法，返回浮点数)
print(10 // 3)    # 3  (除法，整数部分)
print(10 % 3)     # 1  (取模)
print(10 ** 3)    # 1000 (幂运算)
```

#### 浮点数（ float ）
```python
# 浮点数表示
pi = 3.1415926
price = 99.99
scientific_notation = 1.23e5  # 1.23 × 10^5 = 123000.0

# 浮点数精度问题
print(0.1 + 0.2)  # 0.30000000000000004 (精度误差)

# 解决方案：使用decimal模块
from decimal import Decimal
print(Decimal('0.1') + Decimal('0.2'))  # 0.3
```

#### 复数（ complex ）
```python
# 复数表示
z1 = 3 + 4j        # 创建复数
z2 = complex(1, 2)  # 等价于 1 + 2j

# 复数运算
print(z1.real)      # 3.0 (实部)
print(z1.imag)      # 4.0 (虚部)
print(abs(z1))      # 5.0 (模)

# 复数运算
print(z1 + z2)      # (4+6j)
print(z1 * z2)      # (-5+10j)
```

## 📚 序列数据类型

### 字符串（ str ）

#### 字符串创建
```python
# 不同引号表示
single_quotes = 'Hello, Python!'
double_quotes = "Hello, Python!"
triple_quotes = """Hello, Python!"""  # 可跨越多行

# 转义字符
escape_chars = "Line 1\nLine 2\tTab"

# 原始字符串（不转义）
raw_string = r"C:\Users\name\file.txt"
print(raw_string)  # C:\Users\name\file.txt
```

#### 字符串操作
```python
text = "Hello, Python World!"

# 长度
print(len(text))                    # 21

# 大小写转换
print(text.lower())                 # hello, python world!
print(text.upper())                 # HELLO, PYTHON WORLD!
print(text.title())                 # Hello, Python World!

# 字符串查找和替换
print(text.find('Python'))         # 7 (起始位置)
print(text.replace('World', 'Universe'))  # Hello, Python Universe!

# 字符串分割
words = text.split(', ')           # ['Hello', 'Python World!']
print(words[1])                     # Python World!

# 字符串拼接
first_name = "Python"
last_name = "Programming"
full_name = f"{first_name} {last_name}"  # f-string格式化
print(full_name)                    # Python Programming
```

#### 字符串格式化
```python
name = "Python"
version = 3.9

# %格式化
print("Language: %s, Version: %.1f" % (name, version))

# str.format()
print("Language: {}, Version: {}".format(name, version))

# f-string（推荐）
print(f"Language: {name}, Version: {version}")

# 格式化选项
number = 1234.5678
print(f"{number:,.2f}")      # 1,234.57 (千分位)
print(f"{number:.2%}")        # 123456.78% (百分比)
print(f"{number:e}")          # 1.234568e+03 (科学计数法)
```

### 列表（ list ）

#### 列表创建和操作
```python
# 创建列表
empty_list = []
numbers = [1, 2, 3, 4, 5]
mixed = [1, "hello", 3.14, True]
nested = [[1, 2], [3, 4], [5, 6]]

# 列表操作
fruits = ['apple', 'banana', 'cherry']

# 访问元素
print(fruits[0])        # apple (正向索引)
print(fruits[-1])       # cherry (反向索引)
print(fruits[1:3])      # ['banana', 'cherry'] (切片)

# 列表方法
fruits.append('orange')           # 在末尾添加元素
fruits.insert(1, 'grape')         # 在指定位置插入
fruits.remove('banana')          # 删除匹配元素
popped = fruits.pop()             # 删除并返回末尾元素

# 列表推导式
squares = [x**2 for x in range(10)]
print(squares)  # [0, 1, 4, 9, 16, 25, 36, 49, 64, 81]

# 条件列表推导
even_numbers = [x for x in range(10) if x % 2 == 0]
print(even_numbers)  # [0, 2, 4, 6, 8]
```

### 元组（ tuple ）

#### 元组特性和操作
```python
# 创建元组
empty_tuple = ()
single_tuple = (1,)        # 注意逗号！
multi_tuple = (1, 2, 3)
tuple_from_list = tuple([1, 2, 3])

# 元组特性
coordinates = (10, 20)
x, y = coordinates  # 解包
print(f"X: {x}, Y: {y}")  # X: 10, Y: 20

# 元组是不可变的
try:
    coordinates[0] = 15
except TypeError as e:
    print(e)  # 'tuple' object does not support item assignment

# 元组的优势
# 1. 可作为字典键
locations = {
    (10, 20): "Point A",
    (30, 40): "Point B"
}

# 2. 可用于多返回值
def calculate_stats(numbers):
    return min(numbers), max(numbers), sum(numbers) / len(numbers)

data = [1, 2, 3, 4, 5]
minimum, maximum, average = calculate_stats(data)
```

### 🗂️ 集合数据类型

### 集合（ set ）

#### 集合操作
```python
# 创建集合
empty_set = set()
numbers = {1, 2, 3, 4, 5}
unique = set([1, 2, 2, 3, 3, 3])  # {1, 2, 3}

# 集合操作
set_a = {1, 2, 3, 4}
set_b = {3, 4, 5, 6}

# 交集
intersection = set_a & set_b           # {3, 4}
intersection = set_a.intersection(set_b)

# 并集
union_set = set_a | set_b              # {1, 2, 3, 4, 5, 6}
union_set = set_a.union(set_b)

# 差集
difference = set_a - set_b             # {1, 2}
difference = set_a.difference(set_b)

# 对称差集
sym_diff = set_a ^ set_b               # {1, 2, 5, 6}
sym_diff = set_a.symmetric_difference(set_b)

# 集合应用
# 1. 去重
names = ['Alice', 'Bob', 'Alice', 'Charlie']
unique_names = set(names)  # {'Alice', 'Bob', 'Charlie'}

# 2. 快速查找
valid_colors = {'red', 'green', 'blue'}
color = 'red'
if color in valid_colors:
    print(f"{color} is a valid color")
```

### 不可变集合（ frozenset ）
```python
# 创建不可变集合
months = frozenset(['Jan', 'Feb', 'Mar', 'Apr'])
# months.add('May')  # 报错：'frozenset' object has no attribute 'add'
```

### 字典（ dict ）

#### 字典创建和操作
```python
# 创建字典
empty_dict = {}
student = {'name': 'Alice', 'age': 20, 'grade': 'A'}
from_dict = dict(name='Bob', age=25)

# 字典访问
print(student['name'])      # Alice
print(student.get('name')) # Alice
print(student.get('height', 170))  # 170 (默认值)

# 字典操作
student['email'] = 'alice@example.com'  # 添加键值对
student['age'] = 21                       # 更新值
del student['grade']                       # 删除键值对

# 字典方法
print(student.keys())       # 视图对象：dict_keys(['name', 'age', 'email'])
print(student.values())     # 视图对象：dict_values(['Alice', 21, 'alice@example.com'])
print(student.items())      # 视图对象：dict_items([('name', 'Alice'), ...])

# 字典推导式
word_lengths = {word: len(word) for word in ['hello', 'world', 'python']}
print(word_lengths)  # {'hello': 5, 'world': 5, 'python': 6}
```

#### 字典嵌套
```python
# 嵌套字典结构
students = {
    'class_A': {
        'Alice': {'age': 20, 'grade': 'A'},
        'Bob': {'age': 22, 'grade': 'B+'}
    },
    'class_B': {
        'Charlie': {'age': 21, 'grade': 'A-'}
    }
}

# 访问嵌套数据
print(students['class_A']['Alice']['grade'])  # A

# 添加新学生
students['class_B']['David'] = {'age': 23, 'grade': 'B'}
```

## 🔤 类型检查和转换

### 类型检查
```python
# isinstance()函数
x = 10
if isinstance(x, int):
    print("x is an integer")

# 多类型检查
def process_data(data):
    if isinstance(data, (list, tuple)):
        return len(data)
    elif isinstance(data, str):
        return len(data)
    elif isinstance(data, dict):
        return len(data.keys())
    else:
        return 0
```

### 类型转换
```python
# 隐式转换（安全转换）
x = 1 + 2.5  # 3.5 (int自动转为float)

# 显式转换
num_str = "123"
num_int = int(num_str)        # 123 (字符串转整数)
num_float = float(num_str)    # 123.0 (字符串转浮点数)
num_str = str(num_int)        # "123" (整数转字符串)

# 布尔转换
print(bool(0))           # False
print(bool(""))          # False
print(bool([]))          # False
print(bool(None))        # False
print(bool("text"))      # True
print(bool([1, 2, 3]))   # True
```

## 📝 变量命名规范

### 命名规则
```python
# 1. 只能包含字母、数字和下划线
user_name = "Alice"      # ✅
user-name = "Alice"      # ❌

# 2. 不能以数字开头
user1 = "Alice"          # ✅
1 = 1                   # ❌ 语法错误

# 3. 使用有意义的命名
name = "Alice"           # ✅ 自描述
n = "Alice"             # ❌ 不够清晰

# 4. 遵循PEP 8约定
```

### 命名约定
```python
# 变量和函数：小写字母，单词间用下划线
first_name = "Bob"
def calculate_sum(): pass

# 类名：大驼峰命名法
class StudentManager: pass

# 常量：大写字母，单词间用下划线
MAX_AGE = 100
API_URL = "https://api.example.com"

# 私有变量：前导下划线
_private_var = "这是私有的"
class MyClass:
    def __init__(self):
        self._internal_data = []
```

## 🎯 实践练习

### 练习1：学生成绩统计
```python
# 学生数据
students = {
    'Alice': {'math': 85, 'english': 92, 'science': 78},
    'Bob': {'math': 90, 'english': 88, 'science': 85},
    'Charlie': {'math': 76, 'english': 85, 'science': 90}
}

# 计算平均分
def calculate_average(grades):
    return sum(grades.values()) / len(grades)

# 找出最高分
def find_highest_score(scores):
    return max(scores.items(), key=lambda x: x[1])

# 统计结果
averages = {name: calculate_average(grades) for name, grades in students.items()}
print("班级平均分排行榜:")
sorted_averages = sorted(averages.items(), key=lambda x: x[1], reverse=True)
for rank, (name, avg) in enumerate(sorted_averages, 1):
    print(f"第{rank}名: {name}, 平均分: {avg:.2f}")
```

### 练习2：文本分析工具
```python
# 文本数据
text = """
Python is a popular programming language.
Python is known for its simplicity and readability.
Many developers love Python for its capabilities.
"""

# 文本分析函数
def analyze_text(text):
    # 转为小写并分割单词
    words = text.lower().split()
    
    # 统计单词频率
    word_count = {}
    for word in words:
        cleaned_word = word.strip('.,!?')
        word_count[cleaned_word] = word_count.get(cleaned_word, 0) + 1
    
    # 找出最常见词
    most_common = max(word_count.items(), key=lambda x: x[1])
    
    # 统计信息
    return {
        'word_count': len(word_count),
        'character_count': len(text.replace('\n', '').replace(' ', '')),
        'most_common_word': most_common[0],
        'most_common_count': most_common[1]
    }

result = analyze_text(text)
print(f"单词总数: {result['word_count']}")
print(f"字符总数: {result['character_count']}")
print(f"最常见词: '{result['most_common_word']}' (出现{result['most_common_count']}次)")
```

## 📚 最佳实践

### 代码风格
```python
# ✅ 良好的代码风格
import datetime
import json

def process_user_data(users: list) -> dict:
    """
    处理用户数据，统计相关信息
    
    Args:
        users: 用户数据列表
        
    Returns:
        包含统计信息的字典
    """
    processed_data = {}
    
    for user in users:
        user_id = user.get('id', 'unknown')
        if user_id not in processed_data:
            processed_data[user_id] = {
                'count': 0,
                'ages': [],
                'last_updated': datetime.datetime.now()
            }
        
        processed_data[user_id]['count'] += 1
        processed_data[user_id]['ages'].append(user.get('age', 0))
    
    return processed_data
```

### 可读性技巧
```python
# ✅ 使用有意义的变量名
customers = ['Alice', 'Bob', 'Charlie']
active_customers = [c for c in customers if is_active(c)]

# ❌ 避免单字符变量名
a = ['Alice', 'Bob', 'Charlie']
b = [c for c in a if x(c)]

# ✅ 添加注释说明复杂逻辑
# 计算复合年增长率（考虑通货膨胀和投资回报）
def calculate_cagr(initial_value, final_value, years):
    """计算复合年增长率"""
    return (final_value / initial_value) ** (1/years) - 1

# ❌ 没有注释的复杂计算
def calc(val1, val2, n):
    return (val2/val1)**(1/n)-1
```

## 🧪 类型测试工具

```python
def get_type_info(variable):
    """获取变量的类型和详情信息"""
    info = {
        'type': type(variable).__name__,
        'length': len(variable) if hasattr(variable, '__len__') else None,
        'memory_size': sys.getsizeof(variable),
        'methods': [method for method in dir(variable) if not method.startswith('_')]
    }
    
    if isinstance(variable, (list, tuple, set)):
        info['unique_elements'] = len(set(variable))
    
    return info

# 使用示例
import sys
my_list = [1, 2, 3, 2, 1]
print(json.dumps(get_type_info(my_list), indent=2))
```

## 🔄 下一步学习路径

现在你已经掌握了Python的基础语法和数据类型，接下来可以学习：

1. [[控制流与函数]] - 掌握程序逻辑控制
2. [[模块与包管理]] - 学习代码组织方式  
3. [[面向对象编程]] - 进阶到面向对象思维

### 💡 学习建议
- **动手练习**：每个概念都要写验证代码
- **实际应用**：用学到的知识解决小问题
- **代码审查**：比较自己代码和最佳实践的差异
- **循序渐进**：不要跳跃学习，打好坚实基础

---

## 🔗 相关资源

- [[控制流与函数]] - 程序流程控制
- [[Python类型系统]] - 深入理解类型
- [[数据结构进阶]] - 复杂数据结构应用

---
*最后更新: 2026-02-01*  
*分类: 3 Resources*
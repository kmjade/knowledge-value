---
title: 智能分类
aliases:
  - Smart Categorization
  - AI分类
tags:
  - quick-action
  - categorization
---

> [!note] 智能分类系统 / Smart Categorization System
> 基于内容分析和标签系统的自动分类建议

## 功能概述 / Feature Overview

智能分类系统通过分析 InBox 内容的标题、标签、时间等信息，自动建议最适合的 PARA 分类位置。

---

## 分类规则 / Categorization Rules
---

### 项目特征 (Projects)

自动识别特征：
- 包含明确时间期限（`截止日期`、`deadline`、`due`）
- 包含行动动词（`完成`、`实现`、`开发`、`构建`）
- 包含阶段性术语（`v1.0`、`beta`、`阶段`、`里程碑`）
- 标签包含 `#project`、`#active`、`#in-progress`

```dataview
TABLE WITHOUT ID
  file.link as "建议项目",
  confidence as "置信度",
  reason as "原因"
FROM "0 Personals/📥 00_InBox"
WHERE
  contains(tags, "#project") OR
  contains(file.name, "完成") OR
  contains(file.name, "实现") OR
  regexmatch("v\\d+\\.\\d+", file.name) OR
  contains(file.name, "deadline") OR contains(file.name, "截止") OR contains(file.name, "due")
SORT file.mtime DESC
```

### 领域特征 (Areas)

自动识别特征：
- 包含持续性术语（`管理`、`维护`、`运营`、`改进`）
- 包含责任领域（`健康`、`财务`、`学习`、`工作`、`家庭`）
- 标签包含 `#area`、`#responsibility`、`#ongoing`

```dataview
TABLE WITHOUT ID
  file.link as "建议领域",
  confidence as "置信度",
  reason as "原因"
FROM "0 Personals/📥 00_InBox"
WHERE
  contains(tags, "#area") OR
  contains(file.name, "管理") OR
  contains(file.name, "维护") OR
  contains(file.name, "运营") OR
  contains(file.name, "健康") OR contains(file.name, "财务") OR contains(file.name, "学习") OR contains(file.name, "工作") OR contains(file.name, "家庭")
SORT file.mtime DESC
```

### 资源特征 (Resources)

自动识别特征：
- 包含学习/参考术语（`学习笔记`、`教程`、`参考`、`资料`）
- 包含内容类型（`book`、`article`、`video`、`course`）
- 标签包含 `#resource`、`#learn`、`#reference`、`#bookmark`

```dataview
TABLE WITHOUT ID
  file.link as "建议资源",
  confidence as "置信度",
  reason as "原因"
FROM "0 Personals/📥 00_InBox"
WHERE
  contains(tags, "#resource") OR
  contains(file.name, "学习") OR
  contains(file.name, "笔记") OR
  contains(file.name, "教程") OR
  contains(tags, "#book") OR
  contains(tags, "#article") OR
  contains(tags, "#video")
SORT file.mtime DESC
```

### 归档特征 (Archives)

自动识别特征：
- 标记为完成（`#done`、`#completed`、`#finished`）
- 超过 30 天未修改且无待办标签
- 标记为废弃（`#archived`、`#obsolete`）

```dataview
TABLE WITHOUT ID
  file.link as "建议归档",
  confidence as "置信度",
  reason as "原因"
FROM "0 Personals/📥 00_InBox"
WHERE
  contains(tags, "#archived") OR
  contains(tags, "#completed") OR
  (file.mtime < date(today) - dur(30 days) AND !contains(tags, "#todo"))
SORT file.mtime DESC
```

---

## 分类建议列表 / Categorization Suggestions
---

### 待分类内容

```dataview
TABLE WITHOUT ID
  file.link as "文件名",
  file.mtime as "修改时间",
  suggestion as "建议分类",
  confidence as "置信度",
  action as "建议操作"
FROM "0 Personals/📥 00_InBox"
WHERE
  file.name != this.file.name AND
  file.name != "📥 00_InBox.md"
SORT file.mtime DESC
LIMIT 20
```

---

## 快速操作 / Quick Actions
---

### 自动分类

使用以下命令触发自动分类分析：

```
/智能分类
```

系统会：
1. 分析所有 InBox 内容
2. 生成分类建议列表
3. 标注置信度（高/中/低）
4. 提供一键分类选项

### 批量分类

```
/批量分类: 1 Projects
```

将所有高置信度的项目建议移动到 Projects 文件夹。

### 确认分类

```
/确认分类: [[文件名]]
```

对单个文件执行分类操作。

---

## 分类置信度 / Confidence Levels

| 级别 | 阈值 | 建议 |
|------|------|------|
| 高 | > 80% | 可以直接分类 |
| 中 | 50-80% | 需要人工确认 |
| 低 | < 50% | 建议手动分类 |

---

> [!tip] 提升分类准确率
> - 添加明确的标签
> - 使用规范化的文件命名
> - 包含关键词（如 `项目`、`学习`、`参考`）
> - 定期更新分类规则

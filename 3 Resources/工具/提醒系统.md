---
title: 提醒系统
aliases:
  - Reminder System
  - 待办提醒
tags:
  - quick-action
  - reminder
---

> [!note] 提醒系统 / Reminder System
> 基于 InBox 内容的智能提醒和待办管理

---

## 提醒类型 / Reminder Types
---

### 截止日期提醒

当文件包含截止日期时自动创建提醒：

```dataview
TABLE WITHOUT ID
  file.link as "文件",
  due_date as "截止日期",
  (date(today) - date(due_date)).days as "剩余天数",
  priority as "优先级"
FROM "0 Personals/📥 00_InBox"
WHERE due_date AND date(due_date) >= date(today)
SORT due_date ASC
```

### 定期提醒

按时间间隔触发的提醒：

```dataview
TABLE WITHOUT ID
  file.link as "任务",
  reminder_frequency as "频率",
  next_reminder as "下次提醒",
  last_completed as "上次完成"
FROM "0 Personals/📥 00_InBox"
WHERE contains(tags, "#recurring")
SORT next_reminder ASC
```

### 过期提醒

标记为过期未处理的内容：

```dataview
TABLE WITHOUT ID
  file.link as "文件",
  due_date as "原截止日期",
  (date(today) - date(due_date)).days as "过期天数",
  action as "建议操作"
FROM "0 Personals/📥 00_InBox"
WHERE due_date AND date(due_date) < date(today) AND !contains(tags, "#completed")
SORT (date(today) - date(due_date)).days DESC
```

### 新内容提醒

InBox 中最近添加的内容（24 小时内）：

```dataview
TABLE WITHOUT ID
  file.link as "新文件",
  file.ctime as "创建时间",
  urgency as "处理建议"
FROM "0 Personals/📥 00_InBox"
WHERE date(file.ctime) >= date(today) - dur(1 days) AND file.name != this.file.name
SORT file.ctime DESC
```

---

## 设置提醒 / Set Reminders
---

### 添加截止日期

在文件 frontmatter 中添加：

```yaml
---
due_date: 2024-01-25
priority: high
---
```

### 添加定期提醒

```yaml
---
reminder_frequency: weekly
reminder_day: Monday
next_reminder: 2024-01-22
---
```

频率选项：
- `daily` - 每天
- `weekly` - 每周（可指定星期）
- `monthly` - 每月（可指定日期）
- `yearly` - 每年

### 快速添加提醒

```
/提醒: [[文件名]] - due: 2024-01-25 - priority: high
```

### 批量添加提醒

```
/批量提醒: due: 2024-01-31 - from: "0 Personals/📥 00_InBox/0-待办"
```

---

## 提醒仪表板 / Reminder Dashboard
---

### 今日提醒

```dataview
TABLE WITHOUT ID
  file.link as "任务",
  time as "时间",
  type as "类型",
  priority as "优先级"
FROM "0 Personals/📥 00_InBox"
WHERE
  (due_date AND date(due_date) = date(today)) OR
  (next_reminder AND date(next_reminder) = date(today)) OR
  (contains(tags, "#today"))
SORT priority ASC, time ASC
```

### 本周提醒

```dataview
TABLE WITHOUT ID
  dateformat(date(due_date), "E") as "星期",
  file.link as "任务",
  due_date as "日期",
  priority as "优先级"
FROM "0 Personals/📥 00_InBox"
WHERE due_date AND
      date(due_date) >= date(today) AND
      date(due_date) < date(today) + dur(7 days)
SORT due_date ASC
```

### 即将到期（3 天内）

```dataview
TABLE WITHOUT ID
  file.link as "任务",
  due_date as "截止日期",
  (date(due_date) - date(today)).days as "剩余天数",
  priority as "优先级"
FROM "0 Personals/📥 00_InBox"
WHERE due_date AND
      date(due_date) >= date(today) AND
      date(due_date) < date(today) + dur(3 days) AND
      !contains(tags, "#completed")
SORT due_date ASC
```

---

## 优先级管理 / Priority Management
---

### 高优先级

```dataview
TABLE WITHOUT ID
  file.link as "任务",
  due_date as "截止日期",
  reason as "原因"
FROM "0 Personals/📥 00_InBox"
WHERE contains(tags, "#urgent") OR priority = "high"
SORT due_date ASC
LIMIT 10
```

### 中优先级

```dataview
TABLE WITHOUT ID
  file.link as "任务",
  due_date as "截止日期"
FROM "0 Personals/📥 00_InBox"
WHERE priority = "medium" AND !contains(tags, "#completed")
SORT due_date ASC
LIMIT 10
```

### 低优先级

```dataview
TABLE WITHOUT ID
  file.link as "任务",
  due_date as "截止日期"
FROM "0 Personals/📥 00_InBox"
WHERE priority = "low" AND !contains(tags, "#completed")
SORT due_date ASC
LIMIT 10
```

---

## 快速操作 / Quick Actions
---

### 标记完成

```
/完成: [[文件名]]
```

自动：
- 添加 `#completed` 标签
- 记录完成时间
- 如果是定期提醒，设置下次提醒时间

### 延迟提醒

```
/延期: [[文件名]] - due: 2024-02-01
```

### 提醒我

```
/提醒我: 今天 14:00 - "整理 InBox"
```

创建临时提醒（不关联文件）。

### 智能提醒

```
/智能提醒
```

系统分析所有 InBox 内容，自动识别需要处理的事项并创建提醒。

---

## 提醒通知 / Reminder Notifications
---

### 每日摘要

每天早上生成提醒摘要：

```
┌────────────────────────────────────────┐
│  📅 今日提醒 - 2024-01-20 (周一)     │
├────────────────────────────────────────┤
│  🔴 高优先级 (2)                       │
│  - [ ] [[项目A]] - 今天截止           │
│  - [ ] [[会议准备]] - 今天 14:00      │
│                                        │
│  🟡 中优先级 (5)                       │
│  - [ ] [[文档整理]] - 本周内          │
│  - [ ] [[学习笔记]] - 周三截止        │
│  ...                                  │
│                                        │
│  📊 统计                               │
│  - 今日待办: 7                         │
│  - 本周到期: 12                        │
│  - 已过期: 3                           │
└────────────────────────────────────────┘
```

### 过期警告

当有过期任务时显示警告：

```
⚠️ 警告：你有 3 个任务已过期

1. [[旧任务1]] - 过期 5 天
2. [[旧任务2]] - 过期 2 天
3. [[旧任务3]] - 过期 1 天
```

---

## 完成追踪 / Completion Tracking
---

### 今日完成

```dataview
TABLE WITHOUT ID
  file.link as "任务",
  completed_date as "完成时间",
  effort as "用时"
FROM "0 Personals/📥 00_InBox"
WHERE contains(tags, "#completed") AND completed_date AND date(completed_date) = date(today)
SORT completed_date DESC
```

### 本周完成

```dataview
TABLE WITHOUT ID
  dateformat(completed_date, "E") as "日期",
  length(rows) as "完成数"
FROM "0 Personals/📥 00_InBox"
WHERE contains(tags, "#completed") AND completed_date AND
      date(completed_date) >= date(today) - dur(7 days)
GROUP BY dateformat(date(completed_date), "E")
SORT completed_date DESC
```

### 完成率

```dataview
TABLE WITHOUT ID
  "本周完成率" as "指标",
  choice(length(rows) > 0,
         round((length(filter(rows, (r) => contains(r.file.tags, "#completed"))) / length(rows)) * 100, 1),
         0) as "百分比"
FROM "0 Personals/📥 00_InBox"
WHERE file.name != this.file.name
GROUP BY true
```

---

## 定期任务模板 / Recurring Task Templates
---

### 每周整理 InBox

```yaml
---
title: "每周整理 InBox"
reminder_frequency: weekly
reminder_day: Sunday
priority: medium
tags:
  - recurring
  - inbox
---
> [!todo] 每周日整理 InBox
> - 检查所有新内容
> - 分类到 PARA
> - 清理过期内容
> - 添加缺失标签
```

### 每月回顾

```yaml
---
title: "月度 PARA 回顾"
reminder_frequency: monthly
reminder_day: 1
priority: low
tags:
  - recurring
  - review
---
> [!todo] 每月 1 号回顾
> - 回顾项目进展
> - 评估领域责任
> - 整理资源库
> - 归档完成项目
```

---

> [!tip] 提醒最佳实践
> - 每天早上查看今日提醒
> - 完成任务后立即标记完成
> - 使用定期提醒维持习惯
> - 定期清理过期提醒
> - 为重要任务设置高优先级

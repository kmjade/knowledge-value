# 指南

# 配置

## 📖 概述

# 知識

## 🌟 GPT-oss:120b-cloud特性

### 🧠 核心能力
| 能力类型 | 詳細描述 | 應用程式場景 |
|----------|----------|----------|
# 分析
| **代碼生成** | 高品質代碼生成和優化 | 編程辅助、代碼審查 |
# 知識
| **多语言支持** | 支持中英文等多种语言 | 国际化應用程式、翻译 |
# 分析

### ⚡ 效能優勢
- **高吞吐量**：每秒處理数千个token
- **低延遲**：平均響應時間<500ms
- **高可用性**：99.9%服务可用性
- **弹性擴展**：根据负载自動扩容

## 🔑 API密钥获取

### 📝 註冊帳戶

#### 1. 访问官网
```bash
# 访问GPT-oss官网
https://cloud.gpt-oss.com
```

#### 2. 創建帳戶
```bash
# 註冊新使用者流程
1. 点击"立即註冊"
2. 填寫邮箱、密碼
3. 驗證邮箱地址
4. 完善个人資訊
5. 選擇套餐計劃
```

#### 3. 实名认证
```bash
# 实名认证所需材料
- 身份证正反面照片
- 联系電話驗證
- 邮箱驗證码
- 人脸识别（可选）
```

# 管理

#### 生成API密钥
```bash
# 生成新API密钥
curl -X POST "https://api.gpt-oss.com/v1/keys" \
  -H "Authorization: Bearer YOUR_MASTER_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Obsidian Integration",
# 知識庫
    "permissions": ["read", "write"],
    "rate_limit": 1000
  }'
```

#### 密钥響應示例
```json
{
  "key_id": "key_123456789",
  "api_key": "sk-gpt-oss-xxxxx-xxxxx-xxxxx",
  "created_at": "2026-01-21T10:00:00Z",
  "expires_at": "2027-01-21T10:00:00Z",
  "permissions": ["read", "write"],
  "rate_limit": 1000
}
```

#### 密钥安全儲存
```bash
# 配置
mkdir -p ~/.config/gpt-oss
chmod 700 ~/.config/gpt-oss

# 創建密钥檔案
cat > ~/.config/gpt-oss/keys.json << EOF
{
  "production": "sk-gpt-oss-prod-xxxxx-xxxxx-xxxxx",
  "development": "sk-gpt-oss-dev-xxxxx-xxxxx-xxxxx",
  "test": "sk-gpt-oss-test-xxxxx-xxxxx-xxxxx"
}
EOF

# 設置檔案权限
chmod 600 ~/.config/gpt-oss/keys.json
```

# 配置

# 配置

#### 環境变量設置
```bash
# ~/.bashrc 或 ~/.zshrc
export GPT_OSS_API_KEY="sk-gpt-oss-xxxxx-xxxxx-xxxxx"
export GPT_OSS_BASE_URL="https://api.gpt-oss.com/v1"
export GPT_OSS_MODEL="gpt-oss-120b-cloud"
export GPT_OSS_TIMEOUT=30
export GPT_OSS_MAX_TOKENS=4096
```

# 配置
```yaml
# ~/.config/gpt-oss/config.yaml
version: "1.0"
environment: "production"

# 配置
api:
  base_url: "https://api.gpt-oss.com/v1"
  api_key: "${GPT_OSS_API_KEY}"
  model: "gpt-oss-120b-cloud"
  timeout: 30
  max_retries: 3
  retry_delay: 1

# 请求参数
defaults:
  temperature: 0.7
  top_p: 0.9
  max_tokens: 4096
  stream: false
  presence_penalty: 0.0
  frequency_penalty: 0.0

# 高级設置
advanced:
  context_length: 8192
  response_format: "auto"
  user_id: "obsidian-user"
  session_id: "default"

# 安全設置
security:
  validate_ssl: true
  check_content_policy: true
  filter_sensitive_data: true
```

# 配置

#### SSL证书驗證
```yaml
# 配置
ssl:
  enabled: true
  verify_peer: true
  verify_host: true
  ca_bundle: "/etc/ssl/certs/ca-certificates.crt"
  cert_file: null
  key_file: null
```

#### 访问控制
```yaml
# 配置
access_control:
  allowed_origins:
    - "http://localhost:3000"
    - "https://yourdomain.com"
  
  allowed_ips:
    - "127.0.0.1"
    - "10.0.0.0/8"
    - "192.168.0.0/16"
  
  rate_limiting:
    enabled: true
    requests_per_minute: 60
    burst_size: 10
    penalty_duration: 300
```

## 🔗 系統整合

### 📝 Obsidian整合

# 配置
```yaml
# 配置
text_generator:
  provider: "gpt-oss"
  api_key: "${GPT_OSS_API_KEY}"
  base_url: "https://api.gpt-oss.com/v1"
  model: "gpt-oss-120b-cloud"
  
  # 生成参数
  temperature: 0.7
  max_tokens: 2000
  top_p: 0.9
  frequency_penalty: 0.0
  presence_penalty: 0.0
  
  # 功能設置
  features:
    auto_complete: true
    smart_suggestions: true
    context_aware: true
    multi_language: true
  
  # 界面設置
  ui:
    show_temperature: true
    show_model_selector: true
    confirm_before_generate: true
    streaming_response: false
```

#### 智能筆記模板
```yaml
# AI增强筆記模板
ai_note_template:
  name: "GPT-oss增强筆記"
  
  fields:
    - name: "title"
      type: "text"
      required: true
      ai_generated: false
    
    - name: "summary"
      type: "textarea"
      required: true
      ai_generated: true
      ai_prompt: "请为标题'{{title}}'生成一个简洁的總結"
    
    - name: "key_points"
      type: "textarea"
      required: true
      ai_generated: true
      ai_prompt: "基于标题'{{title}}'和總結'{{summary}}'，提取3-5个關鍵要點"
    
    - name: "tags"
      type: "text"
      required: false
      ai_generated: true
      ai_prompt: "为标题'{{title}}'推荐3-5个相關標籤，用逗号分隔"
```

### 💻 OpenCode整合

# 配置
```yaml
# 配置
opencode:
  ai_provider: "gpt-oss"
  
  gpt_oss:
    api_key: "${GPT_OSS_API_KEY}"
    base_url: "https://api.gpt-oss.com/v1"
    model: "gpt-oss-120b-cloud"
    
    # 代碼生成参数
    code_generation:
      temperature: 0.3
      max_tokens: 3000
      top_p: 0.9
      presence_penalty: 0.1
      frequency_penalty: 0.1
    
    # 代碼審查参数
    code_review:
      temperature: 0.1
      max_tokens: 2000
      strict_mode: true
      security_check: true
    
    # 文檔生成参数
    documentation:
      temperature: 0.4
      max_tokens: 2500
      style: "professional"
      language: "auto"
```

# 配置
```python
# 配置
class GPTOSSCodeAssistant:
    def __init__(self, api_key, base_url="https://api.gpt-oss.com/v1"):
        self.api_key = api_key
        self.base_url = base_url
        self.model = "gpt-oss-120b-cloud"
    
    def generate_code(self, prompt, language="python", temperature=0.3):
        """生成代碼"""
        system_prompt = f"""你是一个专业的{language}程式员。
请根据需求生成高品質、可读性强的代碼。
代碼應該遵循最佳實踐和编码規範。"""
        
        response = self._call_api(
            system_prompt=system_prompt,
            user_prompt=prompt,
            temperature=temperature
        )
        
        return response
    
    def review_code(self, code, language="python"):
        """代碼審查"""
        system_prompt = f"""你是一个资深的{language}代碼審查专家。
请从以下角度審查代碼：
1. 代碼品質和可读性
2. 效能優化建議
3. 安全性問題
4. 最佳實踐建議"""
        
        response = self._call_api(
            system_prompt=system_prompt,
            user_prompt=f"请審查以下{language}代碼：\n\n{code}",
            temperature=0.1
        )
        
        return response
    
    def explain_code(self, code, language="python"):
        """代碼解釋"""
        system_prompt = f"""你是一个{language}教学专家。
请用简单易懂的语言解釋代碼的功能和原理。
适合不同水平的開發者理解。"""
        
        response = self._call_api(
            system_prompt=system_prompt,
            user_prompt=f"请解釋以下{language}代碼的功能：\n\n{code}",
            temperature=0.5
        )
        
        return response
```

### 🦙 Ollama整合

# 配置
```yaml
# 配置
model_routing:
  # 路由策略
  strategy: "smart_routing"
  
# 配置
  models:
    local:
      provider: "ollama"
      models:
        - "llama2:7b"
        - "codellama:7b"
      cost_per_token: 0.0
      max_tokens: 4096
      speed: "fast"
    
    cloud:
      provider: "gpt-oss"
      models:
        - "gpt-oss-120b-cloud"
      cost_per_token: 0.00002
      max_tokens: 8192
      speed: "medium"
  
  # 路由规则
  routing_rules:
    - condition: "task_type == 'simple_chat'"
      target: "local"
      model: "llama2:7b"
    
    - condition: "task_type == 'code_simple'"
      target: "local"
      model: "codellama:7b"
    
    - condition: "task_type == 'complex_reasoning'"
      target: "cloud"
      model: "gpt-oss-120b-cloud"
    
    - condition: "token_count > 4000"
      target: "cloud"
      model: "gpt-oss-120b-cloud"
    
    - condition: "user_preference == 'quality_first'"
      target: "cloud"
      model: "gpt-oss-120b-cloud"
```

#### 智能路由實現
```python
# 智能路由實現
class SmartModelRouter:
    def __init__(self, routing_config):
        self.config = routing_config
        self.ollama_client = OllamaClient()
        self.gpt_oss_client = GPTOSSClient()
    
    def route_request(self, prompt, context=None):
        """智能路由请求"""
# 分析
        task_type = self._analyze_task_type(prompt)
        token_count = self._estimate_tokens(prompt)
        user_preference = self._get_user_preference()
        
        # 根据路由规则選擇模型
        target_model = self._select_model(
            task_type, token_count, user_preference
        )
        
        # 發送请求
        if target_model["provider"] == "ollama":
            return self.ollama_client.generate(
                model=target_model["name"],
                prompt=prompt,
                context=context
            )
        else:
            return self.gpt_oss_client.generate(
                model=target_model["name"],
                prompt=prompt,
                context=context
            )
    
    def _analyze_task_type(self, prompt):
# 分析
        if "代碼" in prompt or "編程" in prompt:
            if "简单" in prompt or "基礎" in prompt:
                return "code_simple"
            else:
                return "code_complex"
# 分析
            return "complex_reasoning"
        else:
            return "simple_chat"
    
    def _estimate_tokens(self, text):
        """估算token数量"""
        return len(text) // 4  # 简单估算
    
    def _get_user_preference(self):
        """获取使用者偏好"""
        return self.config.get("user_preference", "balanced")
    
    def _select_model(self, task_type, token_count, user_preference):
        """選擇最优模型"""
        for rule in self.config["routing_rules"]:
            condition = rule["condition"]
            if self._evaluate_condition(condition, task_type, token_count, user_preference):
                return {
                    "provider": rule["target"],
                    "name": rule["model"]
                }
        
        # 默认選擇
        return {
            "provider": "local",
            "name": "llama2:7b"
        }
    
    def _evaluate_condition(self, condition, task_type, token_count, user_preference):
        """評估路由条件"""
        # 簡化的条件評估逻辑
        if "task_type" in condition:
            if condition["task_type"] == task_type:
                return True
        if "token_count" in condition:
            if token_count > int(condition["token_count"].split(">")[1]):
                return True
        if "user_preference" in condition:
            if condition["user_preference"] == user_preference:
                return True
        return False
```

# 管理

### 💰 定价策略

#### 计费模式
| 服务类型 | 价格 | 计费方式 | 免費额度 |
|----------|------|----------|----------|
| **API調用** | ¥0.02/1K tokens | 按量计费 | 100K tokens/月 |
| **代碼生成** | ¥0.025/1K tokens | 按量计费 | 50K tokens/月 |
| **文檔處理** | ¥0.015/1K tokens | 按量计费 | 200K tokens/月 |
| **批量處理** | ¥0.018/1K tokens | 按量计费 | 150K tokens/月 |

#### 套餐計劃
```yaml
# 配置
pricing_plans:
  free:
    name: "免費版"
    price: 0
    monthly_quota: 100000
    features:
      - "基礎API访问"
      - "標準響應速度"
      - "社區支持"
    
  professional:
    name: "专业版"
    price: 99
    monthly_quota: 1000000
    features:
      - "优先API访问"
      - "快速響應速度"
      - "高级功能"
      - "電子郵件支持"
    
  enterprise:
    name: "企业版"
    price: 499
    monthly_quota: 10000000
    features:
      - "专享API访问"
      - "极速響應速度"
      - "全部高级功能"
      - "专属客户经理"
      - "定制化服务"
```

### 📈 使用監控

#### 成本監控腳本
```python
# cost_monitor.py
import requests
import json
from datetime import datetime, timedelta

class CostMonitor:
    def __init__(self, api_key, base_url="https://api.gpt-oss.com/v1"):
        self.api_key = api_key
        self.base_url = base_url
    
    def get_usage_stats(self, start_date, end_date):
        """获取使用統計"""
        url = f"{self.base_url}/usage/stats"
        headers = {"Authorization": f"Bearer {self.api_key}"}
        params = {
            "start_date": start_date,
            "end_date": end_date
        }
        
        response = requests.get(url, headers=headers, params=params)
        return response.json()
    
    def calculate_monthly_cost(self, year, month):
        """計算月度成本"""
        start_date = f"{year}-{month:02d}-01"
        end_date = f"{year}-{month:02d}-31"
        
        stats = self.get_usage_stats(start_date, end_date)
        
        total_tokens = stats["total_tokens"]
        cost = self._calculate_cost(total_tokens)
        
        return {
            "month": f"{year}-{month:02d}",
            "total_tokens": total_tokens,
            "cost": cost,
            "free_quota_used": min(total_tokens, stats["free_quota"]),
            "paid_tokens": max(0, total_tokens - stats["free_quota"])
        }
    
    def _calculate_cost(self, tokens):
        """計算成本"""
        if tokens <= 100000:  # 免費额度
            return 0
        else:
            return (tokens - 100000) * 0.00002
    
    def generate_cost_report(self):
        """生成成本報告"""
        today = datetime.now()
        current_month = self.calculate_monthly_cost(today.year, today.month)
        
        # 获取最近6个月的成本
        report = {
            "current_month": current_month,
            "recent_months": []
        }
        
        for i in range(1, 7):
            date = today - timedelta(days=i*30)
            month_cost = self.calculate_monthly_cost(date.year, date.month)
            report["recent_months"].append(month_cost)
        
        return report
```

# 配置
```yaml
# 配置
budget_alerts:
  enabled: true
  
  # 月度预算設置
  monthly_budget:
    amount: 500  # 月度预算限额
    warning_threshold: 0.8  # 80%时告警
    critical_threshold: 0.95  # 95%时告警
  
  # 告警方式
  alert_methods:
    email:
      enabled: true
      recipients:
        - "admin@company.com"
        - "finance@company.com"
      template: "budget_alert_email"
    
    webhook:
      enabled: true
      url: "https://yourcompany.com/webhooks/budget-alert"
      headers:
        Authorization: "Bearer your-webhook-token"
    
    slack:
      enabled: false
      webhook_url: "https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK"
  
  # 告警訊息模板
  templates:
    email:
      subject: "GPT-oss月度预算告警 - {alert_level}"
      body: |
# 管理
        
        您的GPT-oss月度使用情况如下：
        
        - 月度预算：{monthly_budget}元
        - 当前消费：{current_cost}元
        - 使用率：{usage_percentage}%
        - 剩余额度：{remaining_quota} tokens
        
        {alert_message}
        
        请及时關注使用情况，避免超出预算。
        
# 管理
```

## 🚀 高级功能

# 配置

#### 代碼专用模型
```yaml
# 配置
gpt_oss_code:
  model: "gpt-oss-120b-code"
  specialization: "code_generation"
  
  # 代碼生成優化
  code_generation:
    languages:
      - "python"
      - "javascript"
      - "java"
      - "go"
      - "rust"
      - "cpp"
    
    frameworks:
      python:
        - "django"
        - "flask"
        - "fastapi"
        - "pandas"
        - "numpy"
      javascript:
        - "react"
        - "vue"
        - "angular"
        - "nodejs"
        - "express"
    
    quality_settings:
      follow_best_practices: true
      include_comments: true
      add_error_handling: true
      ensure_type_safety: true
```

#### 文檔专用模型
```yaml
# 配置
gpt_oss_docs:
  model: "gpt-oss-120b-docs"
  specialization: "documentation"
  
  # 文檔生成優化
  documentation:
    formats:
      - "markdown"
      - "html"
      - "pdf"
      - "word"
    
    sections:
      required:
        - "title"
        - "introduction"
        - "usage"
        - "examples"
        - "api_reference"
    
    style_guides:
      technical:
        - "formal_tone"
        - "precise_terminology"
        - "clear_structure"
      user_friendly:
        - "conversational_tone"
        - "simple_explanations"
        - "practical_examples"
```

### 🔄 批量處理

#### 批量API調用
```python
# batch_processor.py
import asyncio
import aiohttp
import json
from typing import List, Dict

class BatchProcessor:
    def __init__(self, api_key, base_url="https://api.gpt-oss.com/v1"):
        self.api_key = api_key
        self.base_url = base_url
        self.max_concurrent = 10  # 并发限制
        self.semaphore = asyncio.Semaphore(self.max_concurrent)
    
    async def process_batch(self, requests: List[Dict], model="gpt-oss-120b-cloud"):
        """批量處理请求"""
        tasks = []
        for i, request in enumerate(requests):
            task = self._process_single_request(request, model, i)
            tasks.append(task)
        
        results = await asyncio.gather(*tasks, return_exceptions=True)
        return results
    
    async def _process_single_request(self, request: Dict, model: str, index: int):
        """處理单个请求"""
        async with self.semaphore:
            try:
                async with aiohttp.ClientSession() as session:
                    headers = {
                        "Authorization": f"Bearer {self.api_key}",
                        "Content-Type": "application/json"
                    }
                    
                    payload = {
                        "model": model,
                        "messages": request["messages"],
                        "temperature": request.get("temperature", 0.7),
                        "max_tokens": request.get("max_tokens", 4096)
                    }
                    
                    async with session.post(
                        f"{self.base_url}/chat/completions",
                        headers=headers,
                        json=payload,
                        timeout=30
                    ) as response:
                        result = await response.json()
                        return {
                            "index": index,
                            "status": "success",
                            "result": result
                        }
            
            except Exception as e:
                return {
                    "index": index,
                    "status": "error",
                    "error": str(e)
                }
    
    async def process_text_batch(self, texts: List[str], prompt_template: str):
        """批量處理文本"""
        requests = []
        for i, text in enumerate(texts):
            request = {
                "messages": [
                    {
                        "role": "system",
                        "content": "你是一个文本處理助手。"
                    },
                    {
                        "role": "user",
                        "content": prompt_template.format(text=text)
                    }
                ],
                "temperature": 0.3,
                "max_tokens": 500
            }
            requests.append(request)
        
        results = await self.process_batch(requests)
        return results

# 使用示例
async def main():
    processor = BatchProcessor(api_key="your-api-key")
    
    texts = [
        "Python是一种編程语言",
        "机器學習是人工智能的一个分支",
        "深度學習使用神经網路"
    ]
    
    prompt_template = "请總結以下文本：{text}"
    
    results = await processor.process_text_batch(texts, prompt_template)
    
    for result in results:
        if result["status"] == "success":
            print(f"文本 {result['index']}: {result['result']['choices'][0]['message']['content']}")
        else:
            print(f"處理失败 {result['index']}: {result['error']}")

if __name__ == "__main__":
    asyncio.run(main())
```

### 🔗 API代理

#### 本地API代理
```python
# api_proxy.py
from flask import Flask, request, jsonify
import requests
import os

app = Flask(__name__)

class GPTOSSProxy:
    def __init__(self):
        self.api_key = os.getenv("GPT_OSS_API_KEY")
        self.base_url = "https://api.gpt-oss.com/v1"
        self.cache = {}
        self.cache_enabled = True
    
    def _get_cache_key(self, payload):
        """生成缓存键"""
        import hashlib
        cache_str = json.dumps(payload, sort_keys=True)
        return hashlib.md5(cache_str.encode()).hexdigest()
    
    def _get_from_cache(self, cache_key):
        """从缓存获取結果"""
        if self.cache_enabled and cache_key in self.cache:
            return self.cache[cache_key]
        return None
    
    def _set_cache(self, cache_key, result):
        """設置缓存"""
        if self.cache_enabled:
            self.cache[cache_key] = result
    
    def _call_gpt_oss(self, payload):
        """調用GPT-oss API"""
        headers = {
            "Authorization": f"Bearer {self.api_key}",
            "Content-Type": "application/json"
        }
        
        response = requests.post(
            f"{self.base_url}/chat/completions",
            headers=headers,
            json=payload,
            timeout=30
        )
        
        return response.json()

gpt_oss = GPTOSSProxy()

@app.route("/v1/chat/completions", methods=["POST"])
def chat_completions():
    """代理聊天完成接口"""
    try:
        payload = request.json
        
        # 檢查缓存
        cache_key = gpt_oss._get_cache_key(payload)
        cached_result = gpt_oss._get_from_cache(cache_key)
        if cached_result:
            return jsonify(cached_result)
        
        # 調用API
        result = gpt_oss._call_gpt_oss(payload)
        
        # 設置缓存
        gpt_oss._set_cache(cache_key, result)
        
        return jsonify(result)
    
    except Exception as e:
        return jsonify({"error": str(e)}), 500

@app.route("/health", methods=["GET"])
def health_check():
    """健康檢查"""
    return jsonify({"status": "healthy"})

if __name__ == "__main__":
    app.run(host="0.0.0.0", port=8080, debug=False)
```

# 配置

# 配置
- [ ] 获取并驗證API密钥
- [ ] 設置環境变量
# 配置
- [ ] 測試基礎API調用

# 配置
# 配置
# 配置
# 配置
- [ ] 測試整合功能

# 管理
- [ ] 了解定价策略
- [ ] 設置预算告警
# 部署
# 配置

### 🚀 高级功能
# 配置
# 部署
- [ ] 設置API代理
- [ ] 測試高级功能

## 🎯 下一步

# 配置

1. **📝 在Obsidian中使用GPT-oss**：體驗智能筆記生成
2. **💻 在OpenCode中使用**：享受高品質代碼生成
# 配置
4. **📊 監控使用情况**：跟踪成本和效能

# 配置

---

# 更新
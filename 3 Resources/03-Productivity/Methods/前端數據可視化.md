---
title: 前端數據可視化
interest-level: ⭐⭐⭐⭐⭐
last-reviewed: 2026-01-26
tags:
  - resource/learning
  - tech/frontend
  - tech/visualization
---

# 前端數據可視化

> 使用前端技術創建交互式數據可視化與圖表

---

## 📖 概述

數據可視化是將數據轉換為圖形表示的過程，幫助用戶更直觀地理解數據模式和趨勢。

### 為什麼選擇前端可視化？

| 優勢 | 說明 |
|------|------|
# 查看
| 響應式 | 自適應各種屏幕尺寸 |
# 更新
| 易於整合 | 直接嵌入 Web 應用 |

---

## 🎯 主流圖表庫

### D3.js - 靈活的數據驅動文檔

**特點**: 最靈活、功能最強大

**適用場景**: 自定義圖表、複雜數據可視化

```javascript
import * as d3 from 'd3';

// 創建 SVG 元素
const svg = d3.select('#chart')
  .append('svg')
  .attr('width', width)
  .attr('height', height);

// 繪製柱狀圖
svg.selectAll('rect')
  .data(data)
  .enter()
  .append('rect')
  .attr('x', d => xScale(d.value))
  .attr('y', d => yScale(d.value))
  .attr('width', xScale.bandwidth())
  .attr('height', d => height - yScale(d.value))
  .attr('fill', '#4a90e2');
```

**優點**:
- 完全控制每個元素
- 強大的數據綁定機制
- 豐富的轉換和動畫支持

**缺點**:
- 學習曲線陡峭
# 效率

---

### Chart.js - 簡單易用

**特點**: 語法簡潔、開箱即用

**適用場景**: 標準圖表、快速原型

```javascript
import Chart from 'chart.js/auto';

const ctx = document.getElementById('myChart').getContext('2d');
new Chart(ctx, {
  type: 'bar',
  data: {
    labels: ['一月', '二月', '三月', '四月'],
    datasets: [{
      label: '銷售額',
      data: [12, 19, 3, 5],
      backgroundColor: 'rgba(74, 144, 226, 0.5)',
      borderColor: 'rgba(74, 144, 226, 1)',
      borderWidth: 1
    }]
  },
  options: {
    responsive: true,
    scales: {
      y: { beginAtZero: true }
    }
  }
});
```

**優點**:
- 易於上手
- 良好的文檔和社區支持
- 響應式設計

**缺點**:
- 自定義能力有限
- 效能對大數據集不理想

---

### ECharts - 功能強大的企業級方案

**特點**: 功能豐富、適用於複雜場景

**適用場景**: 企業級應用、複雜交互

```javascript
import * as echarts from 'echarts';

const chart = echarts.init(document.getElementById('main'));
chart.setOption({
  title: { text: 'ECharts 示例' },
  tooltip: {},
  xAxis: { data: ['一月', '二月', '三月', '四月'] },
  yAxis: {},
  series: [{
    name: '銷售額',
    type: 'bar',
    data: [12, 19, 3, 5],
    itemStyle: { color: '#4a90e2' }
  }]
});

// 響應式調整
window.addEventListener('resize', () => chart.resize());
```

**優點**:
- 豐富的圖表類型
- 優秀的效能
- 內置主題和交互

**缺點**:
- 打包體積較大
- API 相對複雜

---

### Recharts - React 生態的首選

**特點**: React 友好、聲明式 API

**適用場景**: React 應用、需要組件化的項目

```jsx
import {
  BarChart,
  Bar,
  XAxis,
  YAxis,
  CartesianGrid,
  Tooltip,
  Legend
} from 'recharts';

const data = [
  { name: '一月', value: 12 },
  { name: '二月', value: 19 },
  { name: '三月', value: 3 },
  { name: '四月', value: 5 }
];

function MyChart() {
  return (
    <BarChart width={600} height={300} data={data}>
      <CartesianGrid strokeDasharray="3 3" />
      <XAxis dataKey="name" />
      <YAxis />
      <Tooltip />
      <Legend />
      <Bar dataKey="value" fill="#4a90e2" />
    </BarChart>
  );
}
```

**優點**:
- 聲明式 API
- 與 React 完美整合
- 響應式設計

**缺點**:
- 僅適用於 React
- 功能不如 ECharts 豐富

---

### Vega / Vega-Lite - 聲明式可視化語法

**特點**: JSON 聲明式語法

# 分析

```javascript
import vegaEmbed from 'vega-embed';

const spec = {
  "$schema": "https://vega.github.io/schema/vega-lite/v5.json",
  "data": {
    "values": [
      {"category": "A", "value": 12},
      {"category": "B", "value": 19},
      {"category": "C", "value": 3}
    ]
  },
  "mark": "bar",
  "encoding": {
    "x": {"field": "category", "type": "nominal"},
    "y": {"field": "value", "type": "quantitative"}
  }
};

vegaEmbed('#vis', spec);
```

**優點**:
- 聲明式語言易於學習
- 可移植性強（JSON 格式）
- 與數據科學工具整合好

**缺點**:
- 靈活性受限
- 不適合高度自定義場景

---

# 指南

### 數據類型與圖表對應

| 數據類型 | 適合的圖表 | 示例場景 |
|----------|------------|----------|
| 時間序列 | 折線圖、面積圖 | 股票走勢、溫度變化 |
| 分類比較 | 柱狀圖、條形圖 | 銷售額對比、投票結果 |
| 佔比 | 餅圖、環形圖、堆疊圖 | 市場份額、預算分配 |
| 分布 | 直方圖、箱線圖 | 年齡分布、成績分布 |
| 相關性 | 散點圖、熱力圖 | 價格與銷量關係 |
| 階層關係 | 樹狀圖、旭日圖 | 組織架構、檔案目錄 |
| 地理數據 | 地圖、地區圖 | 銷售區域分布 |

---

## 🎨 設計原則與最佳實踐

### 1. 選擇正確的圖表類型

**原則**: 圖表應該讓數據最容易被理解

```
錯誤示例：
- 用餅圖顯示超過 5 個類別
- 用折線圖顯示離散類別數據

正確示例：
- 用柱狀圖比較類別數據
- 用折線圖顯示時間趨勢
```

### 2. 保持簡潔

**原則**: 移除所有不必要的元素

```javascript
// ❌ 不必要的網格線和邊框
options: {
  grid: {
    drawBorder: true,
    drawOnChartArea: true,
    drawTicks: true
  }
}

// ✅ 最小化干擾
options: {
  grid: {
    drawBorder: false,
    drawOnChartArea: false,
    drawTicks: false
  }
}
```

### 3. 使用一致的顏色

**原則**: 使用品牌色或顏色板

```javascript
const colors = {
  primary: '#4a90e2',
  success: '#52c41a',
  warning: '#faad14',
  danger: '#f5222d',
  info: '#1890ff'
};
```

### 4. 新增交互提示

**原則**: 懸停時顯示詳細資訊

```javascript
options: {
  plugins: {
    tooltip: {
      callbacks: {
        label: function(context) {
          return `${context.dataset.label}: ${context.parsed.y}`;
        }
      }
    }
  }
}
```

### 5. 響應式設計

**原則**: 圖表應適應容器大小

```javascript
// React 組件示例
function ResponsiveChart({ data }) {
  const containerRef = useRef(null);
  const chartRef = useRef(null);

  useEffect(() => {
    if (containerRef.current) {
      const chart = echarts.init(containerRef.current);
      chart.setOption(data);
      chartRef.current = chart;

      return () => chart.dispose();
    }
  }, [data]);

  return <div ref={containerRef} style={{ width: '100%', height: '400px' }} />;
}
```

---

## 🚀 高級技巧

### 1. 動畫與過渡

```javascript
# 配置
options: {
  animation: {
    duration: 1000,
    easing: 'easeOutQuart'
  }
}

// D3.js 過渡
svg.selectAll('rect')
  .transition()
  .duration(750)
  .ease(d3.easeCubicInOut)
  .attr('height', d => yScale(d.value));
```

### 2. 大數據優化

```javascript
// 使用 Canvas 而非 SVG
// ECharts 示例
option = {
  renderer: 'canvas',  // 對大數據集效能更好
  series: [{
    type: 'bar',
    data: largeDataArray,
    progressive: 1000,  // 漸進式渲染
    progressiveThreshold: 5000
  }]
};
```

# 更新

```javascript
# 更新
setInterval(() => {
  const newData = generateRandomData();
  chart.setOption({
    series: [{
      data: newData
    }]
  });
}, 1000);
```

### 4. 自定義工具提示

```javascript
// Chart.js 自定義 Tooltip
options: {
  plugins: {
    tooltip: {
      backgroundColor: 'rgba(0, 0, 0, 0.8)',
      titleFont: { size: 14 },
      bodyFont: { size: 12 },
      padding: 12,
      cornerRadius: 4,
      callbacks: {
        footer: (tooltipItems) => {
          return '總計: ' + tooltipItems.reduce((sum, item) => sum + item.parsed.y, 0);
        }
      }
    }
  }
}
```

---

## 🔧 實用工具庫

### 配色方案

| 庫 | 說明 |
|------|------|
| [D3 Color Scales](https://github.com/d3/d3-scale-chromatic) | D3 配色方案 |
| [ColorBrewer](https://colorbrewer2.org/) | 色盲友好的配色 |
| [Chroma.js](https://gka.github.io/chroma.js/) | 顏色操作和縮放 |

### 數據處理

| 庫 | 說明 |
|------|------|
| [Lodash](https://lodash.com/) | JavaScript 工具庫 |
| [D3 Array](https://github.com/d3/d3-array) | 數組統計操作 |
| [NumPy](https://numpy.org/) | Python 數據處理（後端） |

### 導出功能

| 庫 | 說明 |
|------|------|
| [html2canvas](https://html2canvas.hertzen.com/) | 截圖為圖片 |
| [jsPDF](https://github.com/parallax/jsPDF) | 生成 PDF |
| [FileSaver.js](https://github.com/eligrey/FileSaver.js) | 儲存檔案 |

---

## 📚 學習資源

### 官方文檔

- [D3.js Documentation](https://github.com/d3/d3/wiki)
- [Chart.js Documentation](https://www.chartjs.org/docs/latest/)
- [Apache ECharts](https://echarts.apache.org/handbook/zh/get-started/)
- [Recharts Documentation](https://recharts.org/en-US/)

# 教程

- [D3.js in Depth](https://www.d3indepth.com/)
- [Observable Plot](https://observablehq.com/@observablehq/plot)
- [Data Visualization with JavaScript](https://www.udemy.com/course/d3jsdatavisualization/)

### 靈感與示例

- [Observable Gallery](https://observablehq.com/collection/@observablehq/visualization)
- [ECharts Examples](https://echarts.apache.org/examples/zh/index.html)
- [D3 Gallery](https://github.com/d3/d3/wiki/Gallery)

---

# 指南

### 項目需求快速決策樹

```
需要自定義圖表？
├─ 是 → 使用 D3.js
└─ 否
   ├─ React 項目？
   │  ├─ 是 → 使用 Recharts
   │  └─ 否
   │     ├─ 需要企業級功能？
   │     │  ├─ 是 → 使用 ECharts
   │     │  └─ 否 → 使用 Chart.js
   └─ 需要聲明式語法？
      └─ 是 → 使用 Vega-Lite
```

### 圖表庫對比表

| 庫 | 難度 | 靈活性 | 效能 | 包大小 | 生態 |
|------|------|--------|------|--------|------|
| D3.js | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 中等 | ⭐⭐⭐⭐⭐ |
| Chart.js | ⭐⭐ | ⭐⭐ | ⭐⭐⭐ | 小 | ⭐⭐⭐⭐ |
| ECharts | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 大 | ⭐⭐⭐⭐ |
| Recharts | ⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | 中等 | ⭐⭐⭐⭐ |
| Vega-Lite | ⭐⭐ | ⭐⭐ | ⭐⭐⭐ | 中等 | ⭐⭐⭐ |

---

## 🔗 相關資源

- [[3 Resources/01-Tech/Data-Science/Data-Science]]
- [[3 Resources/01-Tech/Programming/Programming]]
# 指南

---

> **提示**: 選擇圖表庫時，考慮團隊技術棧、項目需求和長期維護成本

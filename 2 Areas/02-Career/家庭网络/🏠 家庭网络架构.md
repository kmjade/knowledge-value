---
tags:
  - network
  - infrastructure
  - home-lab
created: 2026-01-15
---

# 網路
# Home Network Architecture Implementation Plan

---

# 分析

### 设备清单 / Device Inventory

| 设备类型 / Type | 数量 / Qty | 说明 / Description |
|----------------|------------|-------------------|
| 光猫 (ONT) | 1 | 4口，运营商提供 / 4-port, ISP provided |
| 主路由器 | 1 | PPPoE 拨号，DHCP 服务器 / PPPoE dial, DHCP server |
| 从路由器 | 1 | AP 模式，扩展覆盖 / AP mode, extend coverage |
# 檔案
# 開發
| 智能家居 | 多 | IoT 子网隔离 / IoT subnet isolation |
| 常规设备 | 多 | 主网段接入 / Main subnet access |

# 網路

| 需求 / Requirement | 优先级 / Priority |
|--------------------|--------------------|
| 高速稳定上网 | ⭐⭐⭐⭐⭐ |
| NAS/服务器远程访问 | ⭐⭐⭐⭐ |
| 智能家居隔离 | ⭐⭐⭐ |
| 覆盖全屋 Wi-Fi | ⭐⭐⭐⭐ |
| 设备间快速互通 | ⭐⭐⭐ |

---

# 設計

### 物理拓扑 / Physical Topology

```
┌─────────────────────────────────────────────────────────────┐
│                      互联网 / Internet                      │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
              ┌────────────────┐
              │   光猫 (ONT)   │
              │   4口光猫     │
              └───────┬────┬───┘
                      │    │
           ISP 提供       │    │
           私有IP       │    │
                      │    │
             ┌────────┘    └────────┐
             │                      │
             ▼                      ▼
    ┌────────────────┐     ┌────────────────┐
    │   主路由器     │     │  从路由器 (AP)  │
    │  Main Router   │     │  Secondary AP  │
    │                │     │                │
    │  ┌──────────┐  │     │  ┌──────────┐  │
    │  │  WAN口   │  │     │  │  WAN口   │  │
    │  └──────────┘  │     │  └──────────┘  │
    │                │     │                │
    │  ┌──────────┐  │     │  ┌──────────┐  │
    │  │ LAN口1   │◄─┼─────┼──│ LAN口    │  │
    │  └──────────┘  │     │  └──────────┘  │
    │  ┌──────────┐  │     │                │
    │  │ LAN口2   │─┼────►│  Wi-Fi 覆盖    │
    │  └──────────┘  │     │  Wi-Fi Cover   │
    │  ┌──────────┐  │     │                │
    │  │ LAN口3   │─┼────►│ IoT 设备      │
    │  └──────────┘  │     │  IoT Devices   │
    │  ┌──────────┐  │     └────────────────┘
    │  │ LAN口4   │─┼────►
    │  └──────────┘  │     ┌────────────────┐
    │                │     │  有线设备      │
    │  Wi-Fi 2.4G/5G│     │  Wired Devices │
    │                │     └────────────────┘
    └────────────────┘
```

---

## 🌐 IP 地址规划 / IP Address Planning

### 主网段 (Main Subnet) - 192.168.1.0/24

| IP 地址 / IP Address | 设备 / Device | 说明 / Description |
|---------------------|---------------|-------------------|
| 192.168.1.1 | 主路由器 / Main Router | 网关，DHCP 服务器 / Gateway, DHCP |
| 192.168.1.2-192.168.1.99 | 动态 DHCP / Dynamic Pool | 常规设备 / Regular devices |
| 192.168.1.100 | NAS/存储 / NAS Storage | 固定 IP / Static IP |
| 192.168.1.101 | 服务器 / Server | 固定 IP / Static IP |
| 192.168.1.102 | 从路由器 (AP) / Secondary AP | 固定 IP / Static IP |
| 192.168.1.103-192.168.1.200 | 保留 / Reserved | 其他服务器设备 / Other servers |
# 配置

### IoT 子网 (IoT Subnet) - 192.168.10.0/24 (可选)

| IP 地址 / IP Address | 设备 / Device | 说明 / Description |
|---------------------|---------------|-------------------|
| 192.168.10.1 | 主路由器 VLAN10 | IoT 网关 / IoT Gateway |
| 192.168.10.2-192.168.1.254 | 智能家居 / Smart Home | IoT 设备池 / IoT devices |

---

# 配置

# 配置

#### 简体中文
1. **桥接模式（推荐）**
   - 关闭光猫的 DHCP 和路由功能
   - 光猫仅作为光电转换设备
   - PPPoE 拨号由主路由器完成

2. **路由模式（备选）**
   - 光猫进行 PPPoE 拨号
# 連接
# 配置

#### Traditional Chinese
1. **橋接模式（推薦）**
   - 關閉光貓的 DHCP 和路由功能
   - 光貓僅作為光電轉換設備
   - PPPoE 撥號由主路由器完成

2. **路由模式（備選）**
   - 光貓進行 PPPoE 撥號
   - 主路由器連接光貓 LAN 口
# 配置

#### English
1. **Bridge Mode (Recommended)**
   - Disable ONT's DHCP and routing
   - ONT acts as optical-to-electrical converter only
   - PPPoE dialing handled by main router

2. **Router Mode (Alternative)**
   - ONT handles PPPoE dialing
   - Main router connects to ONT LAN port
   - Requires DMZ or port forwarding

---

# 配置

# 設置

```yaml
# 配置
wan:
  type: pppoe           # PPPoE 拨号
  username: "ISP账号"    # ISP Account
  password: "ISP密码"   # ISP Password
# 設置

lan:
  ip: 192.168.1.1
  netmask: 255.255.255.0
  dhcp:
    start: 192.168.1.2
    limit: 98
    leasetime: 12h

wireless:
  radio0:
    band: 2.4GHz
    channel: auto
    ssid: "Home_2.4G"
    encryption: wpa2-psk
    key: "YourPassword123"

  radio1:
    band: 5GHz
    channel: auto
    ssid: "Home_5G"
    encryption: wpa2-psk
    key: "YourPassword123"
```

# 設置

```
主要 DNS / Primary DNS:   223.5.5.5  (阿里云 DNS / Alibaba)
备用 DNS / Secondary DNS: 119.29.29.29 (腾讯 DNS / Tencent)
或 / Or:
主要 DNS / Primary DNS:   1.1.1.1     (Cloudflare)
备用 DNS / Secondary DNS: 8.8.8.8     (Google)
```

#### 静态 IP 分配 / Static IP Assignment

| MAC 地址 / MAC | 设备 / Device | 固定 IP / Static IP |
|---------------|---------------|--------------------|
| `AA:BB:CC:DD:EE:01` | NAS | 192.168.1.100 |
| `AA:BB:CC:DD:EE:02` | 服务器 | 192.168.1.101 |
| `AA:BB:CC:DD:EE:03` | 从路由器 | 192.168.1.102 |

---

# 配置

# 設置

```
简体中文 / Simplified Chinese:
# 連接
2. 关闭从路由器的 DHCP 功能
# 設置
# 配置
# 設置

繁體中文 / Traditional Chinese:
1. 將從路由器連接到主路由器的 LAN 口
2. 關閉從路由器的 DHCP 功能
3. 設置與主網段不同的 LAN IP (192.168.1.102)
# 配置
5. 設置不同的信道以減少干擾

English:
1. Connect secondary router to main router's LAN port
2. Disable DHCP on secondary router
3. Set different LAN IP (192.168.1.102) within main subnet
4. Configure same or similar Wi-Fi SSID and password
5. Set different channels to reduce interference
```

# 配置

```
┌─────────────────────────────────────────────────────────────┐
# 連接
├─────────────────────────────────────────────────────────────┤
│  光猫 LAN1 ────────► 主路由器 WAN                         │
│  光猫 LAN2 ────────► 主路由器 LAN (桥接模式)              │
│  主路由器 LAN ─────► 从路由器 LAN (AP 模式)               │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  步骤 2 / Step 2: 从路由器 AP 模式 / AP Mode             │
├─────────────────────────────────────────────────────────────┤
# 管理
│  2. 找到"工作模式"或"路由/AP"选项                        │
│  3. 选择"AP 模式"或"接入点模式"                          │
# 儲存
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
# 設置
├─────────────────────────────────────────────────────────────┤
# 設置
│  2. 关闭 DHCP 服务器                                      │
# 設置
# 設置
│  5. 2.4G 信道: 1 或 6 或 11                            │
│  6. 5G 信道: 与主路由器不同                              │
└─────────────────────────────────────────────────────────────┘
```

---

# 配置

### 防火墙规则 / Firewall Rules

```yaml
# 简体中文 / Simplified Chinese
firewall:
  # 默认策略
  default_input: ACCEPT      # 默认接受入站
  default_output: ACCEPT     # 默认接受出站
# 轉發

  # 允许的规则
  rules:
    - name: "允许局域网访问"
      src: "192.168.1.0/24"
      dest: "192.168.1.1"
      action: ACCEPT

# 連接
      state: "ESTABLISHED,RELATED"
      action: ACCEPT

    - name: "允许 ICMP"
      proto: "icmp"
      action: ACCEPT

# Traditional Chinese / 繁體中文
firewall:
  # 預設策略
  default_input: ACCEPT
  default_output: ACCEPT
  default_forward: REJECT

  # 允許的規則
  rules:
    - name: "允許區域網訪問"
      src: "192.168.1.0/24"
      dest: "192.168.1.1"
      action: ACCEPT

    - name: "允許已建立的連接"
      state: "ESTABLISHED,RELATED"
      action: ACCEPT

    - name: "允許 ICMP"
      proto: "icmp"
      action: ACCEPT

# English
firewall:
  # Default policies
  default_input: ACCEPT
  default_output: ACCEPT
  default_forward: REJECT

  # Allowed rules
  rules:
    - name: "Allow LAN access"
      src: "192.168.1.0/24"
      dest: "192.168.1.1"
      action: ACCEPT

    - name: "Allow established connections"
      state: "ESTABLISHED,RELATED"
      action: ACCEPT

    - name: "Allow ICMP"
      proto: "icmp"
      action: ACCEPT
```

# 轉發

| 服务 / Service | 外部端口 / External Port | 内部 IP / Internal IP | 内部端口 / Internal Port |
|----------------|------------------------|-----------------------|------------------------|
| NAS Web | 80 | 192.168.1.100 | 80 |
| NAS SSH | 2222 | 192.168.1.100 | 22 |
| 服务器 SSH | 2201 | 192.168.1.101 | 22 |
| 服务器 Web | 8080 | 192.168.1.101 | 80 |

---

# 配置

# 配置

```yaml
# 創建
# Create separate VLAN for smart home devices

vlan_config:
  VLAN10_IoT:
    id: 10
    name: "IoT_Network"
    subnet: "192.168.10.0/24"
    gateway: "192.168.10.1"
    dhcp:
      start: "192.168.10.2"
      end: "192.168.10.254"

  # VLAN 访问规则
  # VLAN access rules
  firewall:
    - name: "IoT to Internet"
      src: "192.168.10.0/24"
      dest: "any"
      action: ACCEPT

    - name: "Block IoT to Main LAN"
      src: "192.168.10.0/24"
      dest: "192.168.1.0/24"
      action: REJECT
```

# 配置

```yaml
# 設置
# Traffic priority settings

qos:
  # 游戏 / Gaming
  gaming:
    priority: highest
    ports: ["27015-27030", "3478-3480"]

  # 视频流媒体 / Video Streaming
  streaming:
    priority: high
    ports: ["80", "443"]

# 檔案
  download:
    priority: low
    limit: "50%"

# 上傳
  upload:
    max_bandwidth: "80%"
```

---

# 配置

### 光猫 (ONT) / 光貓
- [ ] 切换到桥接模式 / 切換到橋接模式 / Switch to bridge mode
# 資訊

### 主路由器 / Main Router
# 配置
# 設置
- [ ] DHCP 服务器启用 / DHCP 服務器啟用 / DHCP server enabled
- [ ] Wi-Fi SSID 和密码 / Wi-Fi SSID 和密碼 / Wi-Fi credentials set
# 配置
# 設置
- [ ] 静态 IP 分配 / 靜態 IP 分配 / Static IP assignments

### 从路由器 (AP) / Secondary Router
# 連接
- [ ] AP 模式启用 / AP 模式啟用 / AP mode enabled
- [ ] DHCP 功能关闭 / DHCP 功能關閉 / DHCP disabled
# 設置
- [ ] Wi-Fi 与主路由器相同 / Wi-Fi 與主路由器相同
# 優化

### 服务器和 NAS / Servers and NAS
# 配置
# 測試
# 設置

---

## 🐛 故障排除 / Troubleshooting

### 常见问题 / Common Issues

#### 问题 1: 无法上网 / Issue 1: No Internet Access

```
简体中文 / Simplified Chinese:
诊断步骤:
1. 检查光猫电源和光信号
2. 确认 PPPoE 拨号是否成功
# 設置
4. 重启设备

解决方案:
- 光猫拨号: 联系运营商
- 路由器拨号: 检查账号密码
- DNS 问题: 更换为 223.5.5.5 / 8.8.8.8

Traditional Chinese / 繁體中文:
診斷步驟:
1. 檢查光貓電源和光信號
2. 確認 PPPoE 撥號是否成功
3. 檢查 DNS 設置
4. 重啟設備

解決方案:
- 光貓撥號: 聯繫運營商
- 路由器撥號: 檢查賬號密碼
- DNS 問題: 更換為 223.5.5.5 / 8.8.8.8

English:
Troubleshooting Steps:
1. Check ONT power and optical signal
2. Verify PPPoE dialing success
3. Check DNS settings
4. Restart devices

Solutions:
- ONT dialing: Contact ISP
- Router dialing: Verify account credentials
- DNS issue: Change to 223.5.5.5 / 8.8.8.8
```

#### 问题 2: Wi-Fi 信号弱 / Issue 2: Weak Wi-Fi Signal

```
解决方案 / Solutions:
- 调整路由器位置，避免障碍物
- 使用 5GHz 频段获得更快速度
- 使用 2.4GHz 频段获得更好覆盖
# 系統

解決方案:
- 調整路由器位置，避免障礙物
- 使用 5GHz 頻段獲得更快速度
- 使用 2.4GHz 頻段獲得更好覆蓋
- 考慮 Mesh 系統或增加更多 AP

Solutions:
- Relocate router to avoid obstacles
- Use 5GHz for faster speeds
- Use 2.4GHz for better coverage
- Consider Mesh system or add more APs
```

#### 问题 3: IP 地址冲突 / Issue 3: IP Address Conflict

```
解决方案 / Solutions:
# 設置
- 检查 DHCP 地址池
# 查看
# 修改

解決方案:
- 檢查設備靜態 IP 設置
- 檢查 DHCP 地址池
# 查看
# 修改

Solutions:
- Check device static IP settings
- Check DHCP address pool
- Use arp -a to find conflicting device
- Change conflicting device's IP
```

#### 问题 4: NAS 无法访问 / Issue 4: NAS Cannot Be Accessed

```
诊断步骤:
1. ping NAS IP: ping 192.168.1.100
# 設置
3. 检查防火墙规则
# 設置

解決方案:
- 確認 NAS IP 正確
- 檢查 NAS 網絡設置
- 檢查防火牆規則
- 檢查端口轉發設置

Solutions:
- Verify correct NAS IP
- Check NAS network settings
- Check firewall rules
- Check port forwarding settings
```

---

# 優化

# 優化

# 專案
|-------------|-------------------|
# 設置
| **信道选择** | 2.4G 使用 1/6/11，避免邻道干扰 |
# 下載
| **DNS 选择** | 优先使用国内 DNS，降低延迟 |
| **定期重启** | 每月重启路由器，保持稳定 |

### 设备建议 / Device Recommendations

| 用途 / Use Case | 推荐 / Recommendation |
|-----------------|---------------------|
| 主路由器 | 支持千兆，带 USB 接口 |
| 从路由器/AP | 支持 802.11ac/ax，千兆口 |
| NAS | 千兆网口，支持 RAID |
| 网线 | 超五类 (Cat5e) 或六类 (Cat6) |

---

# 資源

# 文檔
# 配置
- [小米路由器帮助](https://miwifi.com/)
# 配置

---

# 更新

# 版本
|-------------|----------------|-------------------|
# 版本

---

*使用简体中文、繁體中文、英文维护 / Maintained in Simplified Chinese, Traditional Chinese, English*

---
---
根据用户请求自动选择对应的 Obsidian skill：

- `.md` 文件或 Markdown 语法（wikilinks、callouts、embeds、frontmatter）→ 使用 obsidian-markdown skill
- `.base` 文件或数据库视图（表格、卡片、过滤、公式）→ 使用 obsidian-bases skill
- `.canvas` 文件或画布（节点、连线、思维导图）→ 使用 json-canvas skill

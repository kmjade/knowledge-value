---
title:
creation: 2026-01-08T10:07:00
type: ""
status: inbox
outcome: ""
action_plan: ""
tags: []
---
## ✅ Ⅰ 简体中文

---

### 1️⃣ 文章 3‑点速览

|       |内容|
|---|---|
| 核心结论  |AI 让记笔记几乎零成本，真正的效率来自**“少记、分层、只保留能直接驱动行动的笔记”**；把所有原始信息先放在 Raw 层，只有符合 _未来使用场景 / 价值转变 / 单一钩子_ 三条规则的才搬进 Core 层（Projects / Areas / Resources）。|
| 潜在争议  |有人认为大量原始素材本身就是资产，可以随时挖掘、喂给模型提升质量；如果严格删减，可能错失某些偶发但关键的灵感或背景信息。|
| 可行动建议 |建立四层工作流：  <br>1️⃣ 捕获 → QuickAdd 把一切原始内容投进 `0 Personals/📥 00_InBox`。  <br>2️⃣ AI 初筛 → 用 `gpt‑oss:120b‑cloud` 生成 3‑点摘要（结论、争议、行动）。  <br>3️⃣ 24 h 二次筛选 → 运行 `moveToCore.js`，只把满足三条规则的笔记搬到 `01_Core`（对应的 Projects / Areas / Resources）。  <br>4️⃣ 周度归档 → `autoArchive.js` 自动把 `status: done` 且 >30 天的笔记移至 `04_Archives`。每周一次，保持“第二大脑”轻盈。|

---

### 2️⃣ 四张卡片（模板）

> 下面的 Markdown 块可以直接复制到 Obsidian，保存为模板（放在 `_templates/`），随后在 QuickAdd / Templater 中调用。

#### 📌 Problem Card – 问题卡

markdown

```
# 📌 Problem Card – {{tp.file.title}}

## 痛点描述
- 这里写出现的具体困扰（比如「写文章时找不到合适的笔记」）。
- 触发情境是什么？（打开 Obsidian、检索关键词等）

## 受影响范围
- 影响的项目、领域或资源（列出关联的 PARA 项目/Area/Resource）。
- 频率 / 丢失的时间成本（如「每次搜索要花 10‑15 min」）。
```

```
# 📌 Problem Card – {{tp.file.title}}

## 痛点描述
- 这里写出现的具体困扰（比如「写文章时找不到合适的笔记」）。
- 触发情境是什么？（打开 Obsidian、检索关键词等）

## 受影响范围
- 影响的项目、领域或资源（列出关联的 PARA 项目/Area/Resource）。
- 频率 / 丢失的时间成本（如「每次搜索要花 10‑15 min」）。
```

#### 💡 Concept Card – 概念卡

markdown

```
# 💡 Concept Card – {{tp.file.title}}

## 核心概念
- **少记**：只保留能直接驱动行动的内容。
- **分层**：Raw → Core → Archive 三层结构。
- **AI 辅助**：用 `gpt‑oss:120b‑cloud` 自动生成 3‑点摘要。

## 三条筛选规则（Three Rules）
1. **未来使用钩子**：我在何种具体情境下会再次打开它？
2. **价值转变**：这条信息是否会改变我的想法或行动？
3. **单一钩子**：笔记只围绕 **一个** “问题‑结论‑行动”。
```

```
# 💡 Concept Card – {{tp.file.title}}

## 核心概念
- **少记**：只保留能直接驱动行动的内容。
- **分层**：Raw → Core → Archive 三层结构。
- **AI 辅助**：用 `gpt‑oss:120b‑cloud` 自动生成 3‑点摘要。

## 三条筛选规则（Three Rules）
1. **未来使用钩子**：我在何种具体情境下会再次打开它？
2. **价值转变**：这条信息是否会改变我的想法或行动？
3. **单一钩子**：笔记只围绕 **一个** “问题‑结论‑行动”。
```

#### ✅ Solution Card – 方案卡

markdown

```
# ✅ Solution Card – {{tp.file.title}}

## 解决步骤（PARA 工作流）
1️⃣ **Capture** – QuickAdd → `0 Personals/📥 00_InBox/{{tp.file.title}}.md`  
2️⃣ **AI Summary** – 在该文件中运行 Templater `note-summary.md`（调用 `gpt‑oss:120b‑cloud`），得到 3‑点摘要。  
3️⃣ **24 h Review** – 次日运行 `moveToCore.js`，自动把满足三条规则的笔记搬到：  
   - `01_Core/Projects/…`（如属于具体项目）  
   - `01_Core/Areas/…`（如属于长期领域）  
   - `01_Core/Resources/…`（如仅作参考）  
4️⃣ **Weekly Archive** – 在 `09_Weekly Review.md` 中嵌入 `![[/_meta_/scripts/autoArchive.js]]`，每周自动归档完成且旧的笔记。

## 工具链
- **Obsidian**（≥ 1.10.6）  
- **QuickAdd v2.9.4**、**Templater v2.16.4**、**Dataview v0.5.68**、**Obsidian Custom JS**  
- **Ollama** + **模型 `gpt‑oss:120b‑cloud`**（本地代理，HTTP 127.0.0.1:11434）
```

```
# ✅ Solution Card – {{tp.file.title}}

## 解决步骤（PARA 工作流）
1️⃣ **Capture** – QuickAdd → `0 Personals/📥 00_InBox/{{tp.file.title}}.md`  
2️⃣ **AI Summary** – 在该文件中运行 Templater `note-summary.md`（调用 `gpt‑oss:120b‑cloud`），得到 3‑点摘要。  
3️⃣ **24 h Review** – 次日运行 `moveToCore.js`，自动把满足三条规则的笔记搬到：  
   - `01_Core/Projects/…`（如属于具体项目）  
   - `01_Core/Areas/…`（如属于长期领域）  
   - `01_Core/Resources/…`（如仅作参考）  
4️⃣ **Weekly Archive** – 在 `09_Weekly Review.md` 中嵌入 `![[/_meta_/scripts/autoArchive.js]]`，每周自动归档完成且旧的笔记。

## 工具链
- **Obsidian**（≥ 1.10.6）  
- **QuickAdd v2.9.4**、**Templater v2.16.4**、**Dataview v0.5.68**、**Obsidian Custom JS**  
- **Ollama** + **模型 `gpt‑oss:120b‑cloud`**（本地代理，HTTP 127.0.0.1:11434）
```

#### 🔁 Review Card – 复盘卡

markdown

```
# 🔁 Review Card – {{tp.file.title}}

## 本轮执行结果
- **捕获数量**：___ 条  
- **AI 生成摘要**：___ 条（通过 / 未通过）  
- **搬入 Core**：___ 条（Projects / Areas / Resources）  
- **归档**：___ 条

## 发现的问题
- 例：某些笔记的标题不够明确，导致 AI 摘要不准。  
- 例：归类时仍有歧义（Project vs Area）。

## 改进措施
- 为所有新笔记加统一前缀（如 `P_`、`A_`、`R_`），方便后期脚本识别。  
- 在 `note-summary.md` 中加入 **“是否为资源型”** 的二选一提示，帮助 AI 判断。

## 下周期目标
- 完成 **剩余 30 天内未整理的 150 条** 笔记的二次筛选。  
- 优化 `moveToCore.js` 分类规则，使其依据 `#tag` 自动决定子文件夹。
```

```
# 🔁 Review Card – {{tp.file.title}}

## 本轮执行结果
- **捕获数量**：___ 条  
- **AI 生成摘要**：___ 条（通过 / 未通过）  
- **搬入 Core**：___ 条（Projects / Areas / Resources）  
- **归档**：___ 条

## 发现的问题
- 例：某些笔记的标题不够明确，导致 AI 摘要不准。  
- 例：归类时仍有歧义（Project vs Area）。

## 改进措施
- 为所有新笔记加统一前缀（如 `P_`、`A_`、`R_`），方便后期脚本识别。  
- 在 `note-summary.md` 中加入 **“是否为资源型”** 的二选一提示，帮助 AI 判断。

## 下周期目标
- 完成 **剩余 30 天内未整理的 150 条** 笔记的二次筛选。  
- 优化 `moveToCore.js` 分类规则，使其依据 `#tag` 自动决定子文件夹。
```

---

### 3️⃣ 实施方案（Obsidian 中的 PARA 管理）

|步骤|操作|目标文件夹|
|---|---|---|
|① 捕获|使用 QuickAdd 宏 “捕获原始素材”，填写标题 & 内容 → 自动生成 `0 Personals/📥 00_InBox/{{title}}.md`。|`0 Personals/📥 00_InBox`|
|② AI 摘要|打开该文件 → Templater → “note‑summary.md”（调用 `gpt‑oss:120b‑cloud`），在文件顶部生成 3‑点摘要。|同上|
|③ 24 h 二次筛选|第二天运行 Custom JS 脚本 `moveToCore.js`。脚本读取 3‑点摘要，判断是否满足三条规则，若满足则移动：  <br>• `01_Core/Projects/…`（若属于具体项目）  <br>• `01_Core/Areas/…`（若属于长期领域）  <br>• `01_Core/Resources/…`（若为参考资料）。|`01_Core/…`|
|④ 周度归档|在 Weekly Review（`09_Weekly Review.md`）底部插入 `![[/_meta_/scripts/autoArchive.js]]`，每周打开时自动把 `status: done` 且 >30 天的笔记搬到 `04_Archives`。|`04_Archives`|
|⑤ 生成学习资料|在 `3 Resources/学习/少记法/` 里新建 `少记·分层·AI 辅助 学习手册.md`（使用下面的 Learning Handbook 模板）。|`3 Resources/学习/少记法`|
|⑥ 生成管理手册|在 `2 Areas/个人知识管理/` 里新建 `PARA 管理手册.md`（使用下面的 Management Handbook 模板）。|`2 Areas/个人知识管理`|

---

### 4️⃣ 学习资料模板（Resources → Learning Handbook）

markdown

```markdown
---
title: "少记·分层·AI 辅助 学习手册"
date: "{{tp.date.now('YYYY-MM-DD')}}"
type: resource
status: active
tags: ["#learning", "#para"]
---

# 少记·分层·AI 辅助 学习手册

## 目标
- 掌握 **少记、分层** 的思路  
- 学会使用 **Ollama + gpt‑oss:120b‑cloud** 为笔记生成 3‑点摘要  
- 熟悉 **PARA** 在 Obsidian 中的实际落地

## 步骤概览
1. **捕获** → QuickAdd → `0 Personals/📥 00_InBox`  
2. **AI 摘要** → Templater `note-summary.md`  
3. **24 h 复审** → `moveToCore.js`  
4. **周度归档** → `autoArchive.js`  

## 常见问题 & 解答
| 问题 | 解答 |
|------|------|
| “AI 摘要太短/不准” | 在 Prompt 中加入 **“请保持每点不超过 30 字”**，并确保原始内容完整粘贴。 |
| “搬错文件夹” | 检查笔记顶部的 **#tag**（如 `#project`、`#area`、`#resource`），脚本会优先使用标签决定目标子文件夹。 |
| “归档后找不到” | 所有归档均保留完整路径，可在 Obsidian **搜索** `path:"04_Archives"` 再次定位。 |
```

```
---
title: "少记·分层·AI 辅助 学习手册"
date: "{{tp.date.now('YYYY-MM-DD')}}"
type: resource
status: active
tags: ["#learning", "#para"]
---

# 少记·分层·AI 辅助 学习手册

## 目标
- 掌握 **少记、分层** 的思路  
- 学会使用 **Ollama + gpt‑oss:120b‑cloud** 为笔记生成 3‑点摘要  
- 熟悉 **PARA** 在 Obsidian 中的实际落地

## 步骤概览
1. **捕获** → QuickAdd → `0 Personals/📥 00_InBox`  
2. **AI 摘要** → Templater `note-summary.md`  
3. **24 h 复审** → `moveToCore.js`  
4. **周度归档** → `autoArchive.js`  

## 常见问题 & 解答
| 问题 | 解答 |
|------|------|
| “AI 摘要太短/不准” | 在 Prompt 中加入 **“请保持每点不超过 30 字”**，并确保原始内容完整粘贴。 |
| “搬错文件夹” | 检查笔记顶部的 **#tag**（如 `#project`、`#area`、`#resource`），脚本会优先使用标签决定目标子文件夹。 |
| “归档后找不到” | 所有归档均保留完整路径，可在 Obsidian **搜索** `path:"04_Archives"` 再次定位。 |
```

---

### 5️⃣ 管理手册模板（Areas → Management Handbook）

markdown

```
---
title: "PARA 管理手册"
date: "{{tp.date.now('YYYY-MM-DD')}}"
type: area
status: active
tags: ["#para", "#management"]
---

# PARA 管理手册（适用于 0 Personals → 01_Core → 04_Archives）

## 1️⃣ 基础目录（一次性创建）
```

```
---
title: "PARA 管理手册"
date: "{{tp.date.now('YYYY-MM-DD')}}"
type: area
status: active
tags: ["#para", "#management"]
---

# PARA 管理手册（适用于 0 Personals → 01_Core → 04_Archives）

## 1️⃣ 基础目录（一次性创建）
```

0 Personals/ 📥 00_InBox/ 01_Projects/ 02_Areas/ 03_Resources/ 04_Archives/

```

## 2️⃣ 关键工作流
| 环节 | 操作 | 关键插件/脚本 |
|------|------|--------------|
| 捕获 | QuickAdd → **Capture Raw** | `Create a raw.js` |
| 初筛 | Templater → **Note Summary** | `note-summary.md` (调用 `gpt‑oss:120b‑cloud`) |
| 二次筛选 | 每日/每次手动或设为 **Daily** → `moveToCore.js` | 检查三条规则 |
| 归档 | 每周打开 `09_Weekly Review.md` → `autoArchive.js` | 自动搬入 `04_Archives` |

## 3️⃣ 维护技巧
- **标签最少化**：只保留跨维度的全局标签（如 `#para`、`#ai`），状态交给文件夹和 front‑matter。  
- **复盘卡**：每完成一次 **moveToCore** 或 **autoArchive**，在对应笔记里插入 **Review Card**，记录搬迁数量与痛点。  
- **模板升级**：如需新增字段，只修改 `_templates/Base/frontmatter.md`，所有新笔记自动继承。

## 4️⃣ 常见错误排查表
| 错误 | 可能原因 | 解决办法 |
|------|----------|----------|
| “找不到 `note-summary.md`” | 模板路径错误或未在 Templater **Folder** 中加入 `_templates`。 | 在 Settings → Templater → Template folder 中添加 `_templates`，确保文件名无误。 |
| “`moveToCore.js` 未搬文件” | **24 h** 未到，或笔记未满足三条规则。 | 手动执行脚本查看控制台日志，确认规则匹配。 |
| “归档后仍显示在 Dashboard” | Dataview 缓存未刷新。 | 打开任意页面后 **Ctrl+R** 强制刷新，或在 `autoArchive.js` 末尾加入 `location.reload()`。 |
```

```

## 2️⃣ 关键工作流
| 环节 | 操作 | 关键插件/脚本 |
|------|------|--------------|
| 捕获 | QuickAdd → **Capture Raw** | `Create a raw.js` |
| 初筛 | Templater → **Note Summary** | `note-summary.md` (调用 `gpt‑oss:120b‑cloud`) |
| 二次筛选 | 每日/每次手动或设为 **Daily** → `moveToCore.js` | 检查三条规则 |
| 归档 | 每周打开 `09_Weekly Review.md` → `autoArchive.js` | 自动搬入 `04_Archives` |

## 3️⃣ 维护技巧
- **标签最少化**：只保留跨维度的全局标签（如 `#para`、`#ai`），状态交给文件夹和 front‑matter。  
- **复盘卡**：每完成一次 **moveToCore** 或 **autoArchive**，在对应笔记里插入 **Review Card**，记录搬迁数量与痛点。  
- **模板升级**：如需新增字段，只修改 `_templates/Base/frontmatter.md`，所有新笔记自动继承。

## 4️⃣ 常见错误排查表
| 错误 | 可能原因 | 解决办法 |
|------|----------|----------|
| “找不到 `note-summary.md`” | 模板路径错误或未在 Templater **Folder** 中加入 `_templates`。 | 在 Settings → Templater → Template folder 中添加 `_templates`，确保文件名无误。 |
| “`moveToCore.js` 未搬文件” | **24 h** 未到，或笔记未满足三条规则。 | 手动执行脚本查看控制台日志，确认规则匹配。 |
| “归档后仍显示在 Dashboard” | Dataview 缓存未刷新。 | 打开任意页面后 **Ctrl+R** 强制刷新，或在 `autoArchive.js` 末尾加入 `location.reload()`。 |
```

---

### 6️⃣ 最终文件结构（示例）

```
MyVault/
├─ 0 Personals/
│   └─ 📥 00_InBox/
│       └─ 📓 少记·分层·AI 辅助.md   ← 初始捕获笔记
├─ 01_Projects/
│   └─ …
├─ 02_Areas/
│   └─ PARA 管理手册.md
├─ 03_Resources/
│   └─ 学习/
│       └─ 少记法/
│           └─ 少记·分层·AI 辅助 学习手册.md
├─ 04_Archives/
│   └─ …
├─ _templates/
│   ├─ Base/frontmatter.md
│   ├─ PARA/note-summary.md
│   ├─ PARA/problem-card.md
│   ├─ PARA/concept-card.md
│   ├─ PARA/solution-card.md
│   └─ PARA/review-card.md
└─ _meta_/scripts/
    ├─ moveToCore.js
    └─ autoArchive.js
```

```
MyVault/
├─ 0 Personals/
│   └─ 📥 00_InBox/
│       └─ 📓 少记·分层·AI 辅助.md   ← 初始捕获笔记
├─ 01_Projects/
│   └─ …
├─ 02_Areas/
│   └─ PARA 管理手册.md
├─ 03_Resources/
│   └─ 学习/
│       └─ 少记法/
│           └─ 少记·分层·AI 辅助 学习手册.md
├─ 04_Archives/
│   └─ …
├─ _templates/
│   ├─ Base/frontmatter.md
│   ├─ PARA/note-summary.md
│   ├─ PARA/problem-card.md
│   ├─ PARA/concept-card.md
│   ├─ PARA/solution-card.md
│   └─ PARA/review-card.md
└─ _meta_/scripts/
    ├─ moveToCore.js
    └─ autoArchive.js
```

执行顺序：  
1️⃣ 用 QuickAdd 捕获 → 2️⃣ 用 Templater 生成 3‑点摘要 → 3️⃣ 24 h 后运行 `moveToCore.js` 自动搬迁 → 4️⃣ 每周打开 `09_Weekly Review.md` 触发 `autoArchive.js` → 5️⃣ 在对应的 Resources 与 Areas 中维护学习手册与管理手册。

---

## 🟢 Ⅱ 繁體中文

---

### 1️⃣ 文章 3 點概覽

||內容|
|---|---|
|核心結論|AI 讓筆記成本降至零，真正的效率在於**「少記、分層、只留下能直接驅動行動的筆記」**；先把所有原始資訊放入 Raw 層，只有符合 _未來使用情境 / 價值轉變 / 單一鉤子_ 三條規則的才搬入 Core 層（Projects / Areas / Resources）。|
|潛在爭議|有人認為大量原始素材本身就是資產，隨時可以挖掘、餵給模型提升品質；過度刪減或許會錯過偶發的關鍵靈感或背景資訊。|
|可行動建議|四步工作流：  <br>1️⃣ 捕獲 → QuickAdd 把原始內容投至 `0 Personals/📥 00_InBox`。  <br>2️⃣ AI 初篩 → 使用 `gpt‑oss:120b‑cloud` 產生 3 點摘要（結論、爭議、行動）。  <br>3️⃣ 24 h 二次篩選 → 執行 `moveToCore.js`，僅把符合三條規則的筆記搬至 `01_Core`（依照 Projects / Areas / Resources）。  <br>4️⃣ 週度歸檔 → `autoArchive.js` 自動把 `status: done` 且 >30 天的筆記搬至 `04_Archives`。每週一次，保持第二大腦輕盈。|

---

### 2️⃣ 四張卡片（模板）

#### 📌 Problem Card – 問題卡

markdown

```
# 📌 Problem Card – {{tp.file.title}}

## 痛點描述
- 具體的困擾（例如「寫文章時找不到相關筆記」）。
- 觸發情境是什麼？（開 Obsidian、搜尋關鍵字 …）

## 受影響範圍
- 涉及的項目、領域或資源（列出對應的 PARA）  
- 頻率 / 損失的時間成本（如「每次搜尋要花 10‑15 分鐘」）。
```

```
# 📌 Problem Card – {{tp.file.title}}

## 痛點描述
- 具體的困擾（例如「寫文章時找不到相關筆記」）。
- 觸發情境是什麼？（開 Obsidian、搜尋關鍵字 …）

## 受影響範圍
- 涉及的項目、領域或資源（列出對應的 PARA）  
- 頻率 / 損失的時間成本（如「每次搜尋要花 10‑15 分鐘」）。
```

#### 💡 Concept Card – 概念卡

markdown

```
# 💡 Concept Card – {{tp.file.title}}

## 核心概念
- **少記**：只保留能直接驅動行動的內容。  
- **分層**：Raw → Core → Archive 三層結構。  
- **AI 輔助**：使用 `gpt‑oss:120b‑cloud` 產生 3 點摘要。

## 三條篩選規則（Three Rules）
1. **未來使用鉤子**：我在什麼具體情境下會再次打開它？  
2. **價值轉變**：此資訊是否會改變我的想法或行動？  
3. **單一鉤子**：筆記只圍繞 **一個** “問題‑結論‑行動”。  
```

```
# 💡 Concept Card – {{tp.file.title}}

## 核心概念
- **少記**：只保留能直接驅動行動的內容。  
- **分層**：Raw → Core → Archive 三層結構。  
- **AI 輔助**：使用 `gpt‑oss:120b‑cloud` 產生 3 點摘要。

## 三條篩選規則（Three Rules）
1. **未來使用鉤子**：我在什麼具體情境下會再次打開它？  
2. **價值轉變**：此資訊是否會改變我的想法或行動？  
3. **單一鉤子**：筆記只圍繞 **一個** “問題‑結論‑行動”。  
```

#### ✅ Solution Card – 方案卡

markdown

```
# ✅ Solution Card – {{tp.file.title}}

## 解决步骤（PARA 工作流）
1️⃣ **Capture** – QuickAdd → `0 Personals/📥 00_InBox/{{tp.file.title}}.md`  
2️⃣ **AI Summary** – 在該文件中執行 Templater `note-summary.md`（呼叫 `gpt‑oss:120b‑cloud`），得到 3 點摘要。  
3️⃣ **24 h Review** – 次日執行 `moveToCore.js`，自動把符合三條規則的筆記搬到：  
   - `01_Core/Projects/…`（屬於具體專案）  
   - `01_Core/Areas/…`（屬於長期領域）  
   - `01_Core/Resources/…`（純參考資料）  
4️⃣ **Weekly Archive** – 在 `09_Weekly Review.md` 中嵌入 `![[/_meta_/scripts/autoArchive.js]]`，每週自動將完成且超過 30 天的筆記歸檔。

## 工具鏈
- **Obsidian**（≥ 1.10.6）  
- **QuickAdd v2.9.4**、**Templater v2.16.4**、**Dataview v0.5.68**、**Obsidian Custom JS**  
- **Ollama** + **模型 `gpt‑oss:120b‑cloud`**（本地代理 127.0.0.1:11434）  
```

```
# ✅ Solution Card – {{tp.file.title}}

## 解决步骤（PARA 工作流）
1️⃣ **Capture** – QuickAdd → `0 Personals/📥 00_InBox/{{tp.file.title}}.md`  
2️⃣ **AI Summary** – 在該文件中執行 Templater `note-summary.md`（呼叫 `gpt‑oss:120b‑cloud`），得到 3 點摘要。  
3️⃣ **24 h Review** – 次日執行 `moveToCore.js`，自動把符合三條規則的筆記搬到：  
   - `01_Core/Projects/…`（屬於具體專案）  
   - `01_Core/Areas/…`（屬於長期領域）  
   - `01_Core/Resources/…`（純參考資料）  
4️⃣ **Weekly Archive** – 在 `09_Weekly Review.md` 中嵌入 `![[/_meta_/scripts/autoArchive.js]]`，每週自動將完成且超過 30 天的筆記歸檔。

## 工具鏈
- **Obsidian**（≥ 1.10.6）  
- **QuickAdd v2.9.4**、**Templater v2.16.4**、**Dataview v0.5.68**、**Obsidian Custom JS**  
- **Ollama** + **模型 `gpt‑oss:120b‑cloud`**（本地代理 127.0.0.1:11434）  
```

#### 🔁 Review Card – 復盤卡

markdown

```
# 🔁 Review Card – {{tp.file.title}}

## 本輪執行結果
- **捕獲筆記數**：___ 條  
- **AI 產出摘要**：___ 條（通過 / 未通過）  
- **搬入 Core**：___ 條（Projects / Areas / Resources）  
- **歸檔**：___ 條

## 發現的問題
- 例：某些筆記標題不夠清晰，導致 AI 摘要不準。  
- 例：分類時仍存在「Project vs Area」的模糊。

## 改進措施
- 為新筆記加統一前綴（如 `P_`、`A_`、`R_`），方便腳本判斷。  
- 在 `note-summary.md` 中加入 **“是否為資源型”** 的二選一提示，協助 AI 判斷。

## 下週目標
- 完成 **剩餘 30 天內未整理的 150 條** 笔记的二次篩選。  
- 優化 `moveToCore.js` 的分類規則，根據 `#tag` 自動決定子資料夾。  
```

```
# 🔁 Review Card – {{tp.file.title}}

## 本輪執行結果
- **捕獲筆記數**：___ 條  
- **AI 產出摘要**：___ 條（通過 / 未通過）  
- **搬入 Core**：___ 條（Projects / Areas / Resources）  
- **歸檔**：___ 條

## 發現的問題
- 例：某些筆記標題不夠清晰，導致 AI 摘要不準。  
- 例：分類時仍存在「Project vs Area」的模糊。

## 改進措施
- 為新筆記加統一前綴（如 `P_`、`A_`、`R_`），方便腳本判斷。  
- 在 `note-summary.md` 中加入 **“是否為資源型”** 的二選一提示，協助 AI 判斷。

## 下週目標
- 完成 **剩餘 30 天內未整理的 150 條** 笔记的二次篩選。  
- 優化 `moveToCore.js` 的分類規則，根據 `#tag` 自動決定子資料夾。  
```

---

### 3️⃣ 實施方案（Obsidian 中的 PARA 管理）

|步驟|操作|目標資料夾|
|---|---|---|
|① 捕獲|使用 QuickAdd 宏 「捕獲原始素材」，填寫標題與內容 → 自動產生 `0 Personals/📥 00_InBox/{{title}}.md`。|`0 Personals/📥 00_InBox`|
|② AI 摘要|開啟該檔案 → Templater → 「note‑summary.md」（呼叫 `gpt‑oss:120b‑cloud`），在文件頭部生成 3 點摘要。|同上|
|③ 24 h 二次篩選|次日執行 Custom JS `moveToCore.js`。腳本檢查 3 點摘要，若符合三條規則則搬動：  <br>• `01_Core/Projects/…`（屬於具體專案）  <br>• `01_Core/Areas/…`（屬於長期領域）  <br>• `01_Core/Resources/…`（純參考資料）。|`01_Core/…`|
|④ 週度歸檔|在 Weekly Review（`09_Weekly Review.md`）底部加 `![[/_meta_/scripts/autoArchive.js]]`，每次開啟時自動把 `status: done` 且 >30 天的筆記搬至 `04_Archives`。|`04_Archives`|
|⑤ 建立學習手冊|在 `3 Resources/學習/少記法/` 新建 `少記·分層·AI 輔助 學習手冊.md`（使用下方學習手冊模板）。|`3 Resources/學習/少記法`|
|⑥ 建立管理手冊|在 `2 Areas/個人知識管理/` 新建 `PARA 管理手冊.md`（使用下方管理手冊模板）。|`2 Areas/個人知識管理`|

---

### 4️⃣ 學習手冊模板（Resources → Learning Handbook）

markdown

```
---
title: "少記·分層·AI 輔助 學習手冊"
date: "{{tp.date.now('YYYY-MM-DD')}}"
type: resource
status: active
tags: ["#learning", "#para"]
---

# 少記·分層·AI 輔助 學習手冊

## 目標
- 掌握 **少記、分層** 的核心思路。  
- 熟悉 **Ollama + gpt‑oss:120b‑cloud** 產生 3 點摘要的操作。  
- 熟練 **PARA** 在 Obsidian 中的落地流程。

## 工作流概覽
1️⃣ **Capture** → QuickAdd → `0 Personals/📥 00_InBox`  
2️⃣ **AI Summary** → Templater `note-summary.md`（生成 3 點摘要）  
3️⃣ **24 h Review** → `moveToCore.js`（搬入 Core）  
4️⃣ **Weekly Archive** → `autoArchive.js`（歸檔）

## 常見問題 & 解答
| 問題 | 解答 |
|------|------|
| AI 摘要太短或不準確 | 在 Prompt 中加入 **「每點不超過 30 字」**，並確保原始內容完整貼上。 |
| 搬錯資料夾 | 確認筆記前置 **#tag**（如 `#project`、`#area`、`#resource`），腳本會優先依標籤決定子資料夾。 |
| 歸檔後找不到 | 所有歸檔保留完整路徑，可在 Obsidian 搜索 `path:"04_Archives"` 再定位。 |
```

```
---
title: "少記·分層·AI 輔助 學習手冊"
date: "{{tp.date.now('YYYY-MM-DD')}}"
type: resource
status: active
tags: ["#learning", "#para"]
---

# 少記·分層·AI 輔助 學習手冊

## 目標
- 掌握 **少記、分層** 的核心思路。  
- 熟悉 **Ollama + gpt‑oss:120b‑cloud** 產生 3 點摘要的操作。  
- 熟練 **PARA** 在 Obsidian 中的落地流程。

## 工作流概覽
1️⃣ **Capture** → QuickAdd → `0 Personals/📥 00_InBox`  
2️⃣ **AI Summary** → Templater `note-summary.md`（生成 3 點摘要）  
3️⃣ **24 h Review** → `moveToCore.js`（搬入 Core）  
4️⃣ **Weekly Archive** → `autoArchive.js`（歸檔）

## 常見問題 & 解答
| 問題 | 解答 |
|------|------|
| AI 摘要太短或不準確 | 在 Prompt 中加入 **「每點不超過 30 字」**，並確保原始內容完整貼上。 |
| 搬錯資料夾 | 確認筆記前置 **#tag**（如 `#project`、`#area`、`#resource`），腳本會優先依標籤決定子資料夾。 |
| 歸檔後找不到 | 所有歸檔保留完整路徑，可在 Obsidian 搜索 `path:"04_Archives"` 再定位。 |
```

---

### 5️⃣ 管理手冊模板（Areas → Management Handbook）

markdown

```
---
title: "PARA 管理手冊"
date: "{{tp.date.now('YYYY-MM-DD')}}"
type: area
status: active
tags: ["#para", "#management"]
---

# PARA 管理手冊（適用於 0 Personals → 01_Core → 04_Archives）

## 1️⃣ 基礎目錄（一次性建立）
```

```
---
title: "PARA 管理手冊"
date: "{{tp.date.now('YYYY-MM-DD')}}"
type: area
status: active
tags: ["#para", "#management"]
---

# PARA 管理手冊（適用於 0 Personals → 01_Core → 04_Archives）

## 1️⃣ 基礎目錄（一次性建立）
```

0 Personals/ 📥 00_InBox/ 01_Projects/ 02_Areas/ 03_Resources/ 04_Archives/

```

## 2️⃣ 關鍵工作流
| 環節 | 操作 | 主要外掛 / 腳本 |
|------|------|----------------|
| 捕獲 | QuickAdd → **Capture Raw** | `Create a raw.js` |
| 初篩 | Templater → **Note Summary** | `note-summary.md`（呼叫 `gpt‑oss:120b‑cloud`） |
| 二次篩選 | 每日或手動執行 → `moveToCore.js` | 檢查三條規則，搬入相應子資料夾 |
| 歸檔 | 每週打開 `09_Weekly Review.md` → `autoArchive.js` | 自動搬入 `04_Archives` |

## 3️⃣ 維護技巧
- **最少標籤**：只保留跨維度的全局標籤（如 `#para`、`#ai`），狀態交給檔案夾與 front‑matter。  
- **復盤卡**：每完成一次 **moveToCore** 或 **autoArchive**，在對應筆記插入 **Review Card**，記錄搬入數量與痛點。  
- **模板升級**：如需新增欄位，只改 `_templates/Base/frontmatter.md`，所有新筆記自動繼承。

## 4️⃣ 常見錯誤排查表
| 錯誤 | 可能原因 | 解決辦法 |
|------|----------|----------|
| 找不到 `note-summary.md` | 模板路徑未加入 Templater 設定 | 在 Settings → Templater → Template folder 加入 `_templates`，確保檔名正確。 |
| `moveToCore.js` 未搬檔 | 24 h 未滿或筆記未滿足三條規則 | 手動執行腳本查看 console log，確認規則匹配。 |
| 歸檔後仍顯示於 Dashboard | Dataview 快取未刷新 | 開任意筆記後 **Ctrl+R** 強制刷新，或在 `autoArchive.js` 末尾加入 `location.reload()`。 |
```

```

## 2️⃣ 關鍵工作流
| 環節 | 操作 | 主要外掛 / 腳本 |
|------|------|----------------|
| 捕獲 | QuickAdd → **Capture Raw** | `Create a raw.js` |
| 初篩 | Templater → **Note Summary** | `note-summary.md`（呼叫 `gpt‑oss:120b‑cloud`） |
| 二次篩選 | 每日或手動執行 → `moveToCore.js` | 檢查三條規則，搬入相應子資料夾 |
| 歸檔 | 每週打開 `09_Weekly Review.md` → `autoArchive.js` | 自動搬入 `04_Archives` |

## 3️⃣ 維護技巧
- **最少標籤**：只保留跨維度的全局標籤（如 `#para`、`#ai`），狀態交給檔案夾與 front‑matter。  
- **復盤卡**：每完成一次 **moveToCore** 或 **autoArchive**，在對應筆記插入 **Review Card**，記錄搬入數量與痛點。  
- **模板升級**：如需新增欄位，只改 `_templates/Base/frontmatter.md`，所有新筆記自動繼承。

## 4️⃣ 常見錯誤排查表
| 錯誤 | 可能原因 | 解決辦法 |
|------|----------|----------|
| 找不到 `note-summary.md` | 模板路徑未加入 Templater 設定 | 在 Settings → Templater → Template folder 加入 `_templates`，確保檔名正確。 |
| `moveToCore.js` 未搬檔 | 24 h 未滿或筆記未滿足三條規則 | 手動執行腳本查看 console log，確認規則匹配。 |
| 歸檔後仍顯示於 Dashboard | Dataview 快取未刷新 | 開任意筆記後 **Ctrl+R** 強制刷新，或在 `autoArchive.js` 末尾加入 `location.reload()`。 |
```

---

### 6️⃣ 完整目錄示例（最終結構）

```
MyVault/
├─ 0 Personals/
│   └─ 📥 00_InBox/
│       └─ 📓 少记·分层·AI 辅助.md          ← 初始捕获稿
├─ 01_Projects/
│   └─ …
├─ 02_Areas/
│   └─ PARA 管理手册.md
├─ 03_Resources/
│   └─ 学习/
│       └─ 少记法/
│           └─ 少记·分层·AI 辅助 学习手册.md
├─ 04_Archives/
│   └─ …
├─ _templates/
│   ├─ Base/frontmatter.md
│   ├─ PARA/note-summary.md
│   ├─ PARA/problem-card.md
│   ├─ PARA/concept-card.md
│   ├─ PARA/solution-card.md
│   └─ PARA/review-card.md
└─ _meta_/scripts/
    ├─ moveToCore.js
    └─ autoArchive.js
```

```
MyVault/
├─ 0 Personals/
│   └─ 📥 00_InBox/
│       └─ 📓 少记·分层·AI 辅助.md          ← 初始捕获稿
├─ 01_Projects/
│   └─ …
├─ 02_Areas/
│   └─ PARA 管理手册.md
├─ 03_Resources/
│   └─ 学习/
│       └─ 少记法/
│           └─ 少记·分层·AI 辅助 学习手册.md
├─ 04_Archives/
│   └─ …
├─ _templates/
│   ├─ Base/frontmatter.md
│   ├─ PARA/note-summary.md
│   ├─ PARA/problem-card.md
│   ├─ PARA/concept-card.md
│   ├─ PARA/solution-card.md
│   └─ PARA/review-card.md
└─ _meta_/scripts/
    ├─ moveToCore.js
    └─ autoArchive.js
```

執行流程：  
1️⃣ QuickAdd 捕獲 → 2️⃣ Templater 產生 3‑點摘要 → 3️⃣ 24 h 後 `moveToCore.js` 自動搬入 Core（Projects / Areas / Resources）→ 4️⃣ 每週 `autoArchive.js` 歸檔 → 5️⃣ 在 Resources 中保存 學習手冊，在 Areas 中保存 管理手冊，並持續使用四張卡片作為筆記的結構化單位。

---

## 🟢 Ⅲ English

---

### 1️⃣ 3‑Point Summary

||Content|
|---|---|
|Core conclusion|AI makes note‑taking virtually free; real efficiency comes from “take fewer notes, layer them, keep only items that directly drive action.” Put _every_ raw input into a Raw layer; only items that satisfy the _future‑use hook, transformative value, single hook_ move into the Core layer (Projects / Areas / Resources).|
|Potential controversy|Some argue that a huge pool of raw material is itself an asset (future model training, occasional insight). Aggressive pruning may discard occasional but valuable context.|
|Actionable suggestion|Four‑step workflow:  <br>1️⃣ Capture – QuickAdd drops every raw snippet into `0 Personals/📥 00_InBox`.  <br>2️⃣ AI First‑Pass – Run `note‑summary.md` (calls `gpt‑oss:120b‑cloud`) to generate a 3‑point summary (conclusion, controversy, action).  <br>3️⃣ 24 h Second‑Pass – Execute `moveToCore.js`; only notes meeting the three rules are moved to `01_Core` (to the appropriate Projects / Areas / Resources folder).  <br>4️⃣ Weekly Archive – `autoArchive.js` automatically moves `status: done` notes older than 30 days to `04_Archives`. Run weekly, keep the second brain lean.|

---

### 2️⃣ Four Card Templates (Markdown)

#### 📌 Problem Card

markdown

```
# 📌 Problem Card – {{tp.file.title}}

## Pain point description
- Describe the concrete issue (e.g., “Can’t find the right note when writing an article”).  
- When does it happen? (Opening Obsidian, searching a keyword, etc.)

## Affected scope
- Which Projects / Areas / Resources does it impact?  
- Frequency / time cost lost (e.g., “10‑15 min per search”).  
```

```
# 📌 Problem Card – {{tp.file.title}}

## Pain point description
- Describe the concrete issue (e.g., “Can’t find the right note when writing an article”).  
- When does it happen? (Opening Obsidian, searching a keyword, etc.)

## Affected scope
- Which Projects / Areas / Resources does it impact?  
- Frequency / time cost lost (e.g., “10‑15 min per search”).  
```

#### 💡 Concept Card

markdown

```
# 💡 Concept Card – {{tp.file.title}}

## Core ideas
- **Less‑note**: keep only content that directly drives action.  
- **Layering**: Raw → Core → Archive three‑layer model.  
- **AI assistance**: use `gpt‑oss:120b‑cloud` to generate a concise 3‑point summary.

## Three Rules
1. **Future hook** – In which concrete situation will I open this again?  
2. **Transformative value** – Does this information change my thinking or actions?  
3. **Single hook** – The note revolves around ONE “question‑conclusion‑action”.  
```

```
# 💡 Concept Card – {{tp.file.title}}

## Core ideas
- **Less‑note**: keep only content that directly drives action.  
- **Layering**: Raw → Core → Archive three‑layer model.  
- **AI assistance**: use `gpt‑oss:120b‑cloud` to generate a concise 3‑point summary.

## Three Rules
1. **Future hook** – In which concrete situation will I open this again?  
2. **Transformative value** – Does this information change my thinking or actions?  
3. **Single hook** – The note revolves around ONE “question‑conclusion‑action”.  
```

#### ✅ Solution Card

markdown

```
# ✅ Solution Card – {{tp.file.title}}

## PARA workflow steps
1️⃣ **Capture** – QuickAdd → `0 Personals/📥 00_InBox/{{tp.file.title}}.md`  
2️⃣ **AI Summary** – In that file run Templater `note-summary.md` (calls `gpt‑oss:120b‑cloud`) to obtain a 3‑point summary.  
3️⃣ **24 h Review** – Next day run `moveToCore.js`; automatically move notes that satisfy the three rules into:  
   - `01_Core/Projects/...` (if it belongs to a concrete project)  
   - `01_Core/Areas/...` (if it belongs to a long‑term domain)  
   - `01_Core/Resources/...` (if it is pure reference)  
4️⃣ **Weekly Archive** – Embed `![[/_meta_/scripts/autoArchive.js]]` in `09_Weekly Review.md`; each week it archives completed (>30 days) notes.

## Toolchain
- **Obsidian** (≥ 1.10.6)  
- **QuickAdd v2.9.4**, **Templater v2.16.4**, **Dataview v0.5.68**, **Obsidian Custom JS**  
- **Ollama** + model **`gpt‑oss:120b‑cloud`** (local proxy at 127.0.0.1:11434)  
```

```
# ✅ Solution Card – {{tp.file.title}}

## PARA workflow steps
1️⃣ **Capture** – QuickAdd → `0 Personals/📥 00_InBox/{{tp.file.title}}.md`  
2️⃣ **AI Summary** – In that file run Templater `note-summary.md` (calls `gpt‑oss:120b‑cloud`) to obtain a 3‑point summary.  
3️⃣ **24 h Review** – Next day run `moveToCore.js`; automatically move notes that satisfy the three rules into:  
   - `01_Core/Projects/...` (if it belongs to a concrete project)  
   - `01_Core/Areas/...` (if it belongs to a long‑term domain)  
   - `01_Core/Resources/...` (if it is pure reference)  
4️⃣ **Weekly Archive** – Embed `![[/_meta_/scripts/autoArchive.js]]` in `09_Weekly Review.md`; each week it archives completed (>30 days) notes.

## Toolchain
- **Obsidian** (≥ 1.10.6)  
- **QuickAdd v2.9.4**, **Templater v2.16.4**, **Dataview v0.5.68**, **Obsidian Custom JS**  
- **Ollama** + model **`gpt‑oss:120b‑cloud`** (local proxy at 127.0.0.1:11434)  
```

#### 🔁 Review Card

markdown

```
# 🔁 Review Card – {{tp.file.title}}

## Execution results this cycle
- **Captured notes**: ___  
- **AI summaries generated**: ___ (pass / fail)  
- **Moved to Core**: ___ (Projects / Areas / Resources)  
- **Archived**: ___

## Issues discovered
- Example: Some titles were vague, causing poor AI summaries.  
- Example: Still ambiguous between Project vs Area.

## Improvement actions
- Add a uniform prefix (e.g., `P_`, `A_`, `R_`) to new notes for easier script detection.  
- Extend `note-summary.md` with a **“Is this a resource?”** binary prompt to help AI decide placement.

## Next‑week goals
- Finish the remaining 150 notes from the past 30 days that are still unprocessed.  
- Refine `moveToCore.js` classification logic to automatically decide sub‑folder based on `#tag`.  
```

```
# 🔁 Review Card – {{tp.file.title}}

## Execution results this cycle
- **Captured notes**: ___  
- **AI summaries generated**: ___ (pass / fail)  
- **Moved to Core**: ___ (Projects / Areas / Resources)  
- **Archived**: ___

## Issues discovered
- Example: Some titles were vague, causing poor AI summaries.  
- Example: Still ambiguous between Project vs Area.

## Improvement actions
- Add a uniform prefix (e.g., `P_`, `A_`, `R_`) to new notes for easier script detection.  
- Extend `note-summary.md` with a **“Is this a resource?”** binary prompt to help AI decide placement.

## Next‑week goals
- Finish the remaining 150 notes from the past 30 days that are still unprocessed.  
- Refine `moveToCore.js` classification logic to automatically decide sub‑folder based on `#tag`.  
```

---

### 3️⃣ Implementation Plan (PARA in Obsidian)

|Step|Action|Target folder|
|---|---|---|
|① Capture|QuickAdd macro “Capture Raw Material” → fill _title_ and _content_ → auto‑creates `0 Personals/📥 00_InBox/{{title}}.md`.|`0 Personals/📥 00_InBox`|
|② AI Summary|Open the newly created file → Templater → run `note-summary.md` (calls `gpt‑oss:120b‑cloud`) → 3‑point summary appears at top.|Same file|
|③ 24 h Second‑Pass|After 24 h, execute Custom JS script `moveToCore.js`. The script reads the 3‑point summary, checks the three rules, and moves the file into one of:  <br>• `01_Core/Projects/...`  <br>• `01_Core/Areas/...`  <br>• `01_Core/Resources/...`.|`01_Core/…`|
|④ Weekly Archive|In `09_Weekly Review.md` (a Periodic Note) add `![[/_meta_/scripts/autoArchive.js]]`. Opening the weekly note runs the script, moving any `status: done` notes older than 30 days to `04_Archives`.|`04_Archives`|
|⑤ Learning Handbook|Create a new note `3 Resources/Learning/Less‑Notes/Less‑Notes · Layering · AI‑Assist Learning Handbook.md` using the Learning Handbook template below.|`3 Resources/Learning/Less‑Notes`|
|⑥ Management Handbook|Create a new note `2 Areas/Personal Knowledge Management/PARA Management Handbook.md` using the Management Handbook template below.|`2 Areas/Personal Knowledge Management`|

---

### 4️⃣ Learning Handbook (Resource) Template

markdown

```
---
title: "Less‑Notes · Layering · AI‑Assist Learning Handbook"
date: "{{tp.date.now('YYYY-MM-DD')}}"
type: resource
status: active
tags: ["#learning", "#para"]
---

# Less‑Notes · Layering · AI‑Assist Learning Handbook

## Goal
- Master the **Less‑Note & Layering** mindset.  
- Learn to use **Ollama + gpt‑oss:120b‑cloud** to generate a concise 3‑point summary.  
- Apply the **PARA** framework inside Obsidian.

## Step‑by‑step overview
1️⃣ **Capture** – QuickAdd → `0 Personals/📥 00_InBox`.  
2️⃣ **AI Summary** – Run Templater `note-summary.md`.  
3️⃣ **24 h Review** – Run `moveToCore.js`.  
4️⃣ **Weekly Archive** – Run `autoArchive.js`.

## FAQ
| Question | Answer |
|----------|--------|
| “AI summary too short / inaccurate” | Add **“keep each point ≤ 30 characters”** to the prompt and ensure the raw content is fully pasted. |
| “Note moved to the wrong folder” | Verify the note’s **#tag** (`#project`, `#area`, `#resource`) at the top; the script prioritises tags for folder routing. |
| “Can’t find an archived note” | All archived notes keep their full path; search `path:"04_Archives"` to locate them. |
```

```
---
title: "Less‑Notes · Layering · AI‑Assist Learning Handbook"
date: "{{tp.date.now('YYYY-MM-DD')}}"
type: resource
status: active
tags: ["#learning", "#para"]
---

# Less‑Notes · Layering · AI‑Assist Learning Handbook

## Goal
- Master the **Less‑Note & Layering** mindset.  
- Learn to use **Ollama + gpt‑oss:120b‑cloud** to generate a concise 3‑point summary.  
- Apply the **PARA** framework inside Obsidian.

## Step‑by‑step overview
1️⃣ **Capture** – QuickAdd → `0 Personals/📥 00_InBox`.  
2️⃣ **AI Summary** – Run Templater `note-summary.md`.  
3️⃣ **24 h Review** – Run `moveToCore.js`.  
4️⃣ **Weekly Archive** – Run `autoArchive.js`.

## FAQ
| Question | Answer |
|----------|--------|
| “AI summary too short / inaccurate” | Add **“keep each point ≤ 30 characters”** to the prompt and ensure the raw content is fully pasted. |
| “Note moved to the wrong folder” | Verify the note’s **#tag** (`#project`, `#area`, `#resource`) at the top; the script prioritises tags for folder routing. |
| “Can’t find an archived note” | All archived notes keep their full path; search `path:"04_Archives"` to locate them. |
```

---

### 5️⃣ Management Handbook (Area) Template

markdown

```
---
title: "PARA Management Handbook"
date: "{{tp.date.now('YYYY-MM-DD')}}"
type: area
status: active
tags: ["#para", "#management"]
---

# PARA Management Handbook (for 0 Personals → 01 Core → 04 Archives)

## 1️⃣ Folder foundation (create once)
```

```
---
title: "PARA Management Handbook"
date: "{{tp.date.now('YYYY-MM-DD')}}"
type: area
status: active
tags: ["#para", "#management"]
---

# PARA Management Handbook (for 0 Personals → 01 Core → 04 Archives)

## 1️⃣ Folder foundation (create once)
```

0 Personals/ 📥 00_InBox/ 01_Projects/ 02_Areas/ 03_Resources/ 04_Archives/

```

## 2️⃣ Core workflow
| Stage | Action | Key plugin / script |
|-------|--------|---------------------|
| Capture | QuickAdd → **Capture Raw** | `Create a raw.js` |
| First‑pass | Templater → **Note Summary** | `note-summary.md` (calls `gpt‑oss:120b‑cloud`) |
| Second‑pass | Daily / manual → `moveToCore.js` | Checks three rules & moves to proper sub‑folder |
| Archive | Weekly open `09_Weekly Review.md` → `autoArchive.js` | Auto‑moves completed (>30 days) notes to `04_Archives` |

## 3️⃣ Maintenance tips
- **Minimal tags** – Only keep global cross‑dimensional tags (`#para`, `#ai`); status is expressed by folder & front‑matter.  
- **Review Card** – After each `moveToCore` or `autoArchive` run, insert a **Review Card** in the affected note to log numbers and pain points.  
- **Template upgrades** – If you need new front‑matter fields, edit `_templates/Base/frontmatter.md`; all new notes inherit the changes automatically.

## 4️⃣ Common error checklist
| Error | Likely cause | Fix |
|-------|--------------|-----|
| “Can’t find `note-summary.md`” | Template folder not added to Templater settings. | Add `_templates` to *Settings → Templater → Template folder*. |
| “`moveToCore.js` didn’t move anything” | 24 h not reached or note fails the three‑rule test. | Run script manually, check console logs for rule mismatches. |
| “Archived notes still appear on dashboard” | Dataview cache not refreshed. | Press **Ctrl+R** or add `location.reload()` at the end of `autoArchive.js`. |
```

```

## 2️⃣ Core workflow
| Stage | Action | Key plugin / script |
|-------|--------|---------------------|
| Capture | QuickAdd → **Capture Raw** | `Create a raw.js` |
| First‑pass | Templater → **Note Summary** | `note-summary.md` (calls `gpt‑oss:120b‑cloud`) |
| Second‑pass | Daily / manual → `moveToCore.js` | Checks three rules & moves to proper sub‑folder |
| Archive | Weekly open `09_Weekly Review.md` → `autoArchive.js` | Auto‑moves completed (>30 days) notes to `04_Archives` |

## 3️⃣ Maintenance tips
- **Minimal tags** – Only keep global cross‑dimensional tags (`#para`, `#ai`); status is expressed by folder & front‑matter.  
- **Review Card** – After each `moveToCore` or `autoArchive` run, insert a **Review Card** in the affected note to log numbers and pain points.  
- **Template upgrades** – If you need new front‑matter fields, edit `_templates/Base/frontmatter.md`; all new notes inherit the changes automatically.

## 4️⃣ Common error checklist
| Error | Likely cause | Fix |
|-------|--------------|-----|
| “Can’t find `note-summary.md`” | Template folder not added to Templater settings. | Add `_templates` to *Settings → Templater → Template folder*. |
| “`moveToCore.js` didn’t move anything” | 24 h not reached or note fails the three‑rule test. | Run script manually, check console logs for rule mismatches. |
| “Archived notes still appear on dashboard” | Dataview cache not refreshed. | Press **Ctrl+R** or add `location.reload()` at the end of `autoArchive.js`. |
```

---

### 6️⃣ Final Folder Layout (example)

```
MyVault/
├─ 0 Personals/
│   └─ 📥 00_InBox/
│       └─ 📓 Less‑Notes · Layering · AI‑Assist.md   ← raw capture
├─ 01_Projects/
│   └─ …
├─ 02_Areas/
│   └─ PARA Management Handbook.md
├─ 03_Resources/
│   └─ Learning/
│       └─ Less‑Notes/
│           └─ Less‑Notes · Layering · AI‑Assist Learning Handbook.md
├─ 04_Archives/
│   └─ …
├─ _templates/
│   ├─ Base/frontmatter.md
│   ├─ PARA/note-summary.md
│   ├─ PARA/problem-card.md
│   ├─ PARA/concept-card.md
│   ├─ PARA/solution-card.md
│   └─ PARA/review-card.md
└─ _meta_/scripts/
    ├─ moveToCore.js
    └─ autoArchive.js
```

```
MyVault/
├─ 0 Personals/
│   └─ 📥 00_InBox/
│       └─ 📓 Less‑Notes · Layering · AI‑Assist.md   ← raw capture
├─ 01_Projects/
│   └─ …
├─ 02_Areas/
│   └─ PARA Management Handbook.md
├─ 03_Resources/
│   └─ Learning/
│       └─ Less‑Notes/
│           └─ Less‑Notes · Layering · AI‑Assist Learning Handbook.md
├─ 04_Archives/
│   └─ …
├─ _templates/
│   ├─ Base/frontmatter.md
│   ├─ PARA/note-summary.md
│   ├─ PARA/problem-card.md
│   ├─ PARA/concept-card.md
│   ├─ PARA/solution-card.md
│   └─ PARA/review-card.md
└─ _meta_/scripts/
    ├─ moveToCore.js
    └─ autoArchive.js
```

Workflow Recap

1️⃣ Capture → QuickAdd → raw note in `0 Personals/📥 00_InBox`.  
2️⃣ AI First‑Pass → Templater `note-summary.md` → 3‑point summary.  
3️⃣ 24 h Second‑Pass → `moveToCore.js` → move to appropriate `01_Core` sub‑folder.  
4️⃣ Weekly Archive → `autoArchive.js` → move old `done` notes to `04_Archives`.  
5️⃣ Learning & Management Handbook → keep in Resources and Areas, respectively, and use them as reference for future notes.

---

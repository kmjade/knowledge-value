---
title: 代码示例
aliases:
  - Gemini 代码示例
  - Gemini 示例代码
tags:
  - #topic/gemini
  - #topic/api
  - #type/reference
created: 2026-05-25
updated: 2026-05-25
---

# 代码示例

## 基础示例

### 文本生成

```python
import google.generativeai as genai

genai.configure(api_key="YOUR_API_KEY")
model = genai.GenerativeModel('gemini-2.5-pro')

# 简单生成
response = model.generate_content("写一首关于春天的诗")
print(response.text)
```

### 流式输出

```python
response = model.generate_content("写一个长故事", stream=True)
for chunk in response:
    print(chunk.text, end="", flush=True)
```

---

## 多轮对话

### 基本聊天

```python
chat = model.start_chat(history=[])

while True:
    user_input = input("你: ")
    if user_input.lower() in ['exit', 'quit']:
        break

    response = chat.send_message(user_input)
    print(f"Gemini: {response.text}")
```

### 带系统提示的聊天

```python
model = genai.GenerativeModel(
    'gemini-2.5-pro',
    system_instruction="你是一位专业的Python编程导师，用简洁易懂的方式回答问题。"
)

chat = model.start_chat()
response = chat.send_message("如何学习Python？")
print(response.text)
```

---

## 多模态示例

### 图像分析

```python
import PIL.Image

image = PIL.Image.open("screenshot.png")

model = genai.GenerativeModel('gemini-2.5-pro')
response = model.generate_content([
    "分析这张截图，描述其中展示的内容",
    image
])
print(response.text)
```

### OCR 提取

```python
image = PIL.Image.open("document.png")

response = model.generate_content([
    "提取图片中的所有文字，保持原有格式",
    image
])
print(response.text)
```

### 图表分析

```python
chart_image = PIL.Image.open("chart.png")

response = model.generate_content([
    """分析这张图表：
    1. 描述图表类型和主要内容
    2. 总结关键数据和趋势
    3. 给出数据洞察和建议
    """,
    chart_image
])
print(response.text)
```

---

## 代码助手

### 代码审查

```python
code = '''
def process_data(data):
    result = []
    for item in data:
        if item > 0:
            result.append(item * 2)
    return result
'''

response = model.generate_content(f"""
请审查以下代码：
1. 代码质量评价
2. 潜在问题
3. 改进建议
4. 重构后的代码

```python
{code}
```
""")
print(response.text)
```

### 生成测试

```python
code_to_test = '''
def calculate_average(numbers):
    if not numbers:
        return 0
    return sum(numbers) / len(numbers)
'''

response = model.generate_content(f"""
为以下函数生成完整的单元测试，使用 pytest 框架：

```python
{code_to_test}
```

要求：
1. 测试正常情况
2. 测试边界情况
3. 测试异常情况
""")
print(response.text)
```

### 代码解释

```python
complex_code = '''
def fibonacci(n):
    return n if n <= 1 else fibonacci(n-1) + fibonacci(n-2)
'''

response = model.generate_content(f"""
解释这段代码的工作原理，包括：
1. 函数功能
2. 执行流程
3. 时间复杂度
4. 潜在问题

```python
{complex_code}
```
""")
print(response.text)
```

---

## 文档处理

### 文档总结

```python
with open("article.txt", "r") as f:
    content = f.read()

response = model.generate_content(f"""
总结以下文档，要求：
1. 提取核心观点
2. 列出关键信息
3. 给出结论

文档内容：
{content}
""")
print(response.text)
```

### 文档问答

```python
document = open("report.pdf", "rb").read()

chat = model.start_chat()
chat.send_message(f"这是文档内容，请记住：{document}")

# 提问
response = chat.send_message("这份报告的主要结论是什么？")
print(response.text)
```

---

## 内容生成

### 文章写作

```python
response = model.generate_content("""
写一篇关于人工智能未来发展的文章，要求：
1. 标题吸引人
2. 字数约 800 字
3. 包含以下部分：
   - 引言
   - 现状分析
   - 未来展望
   - 结论
""")
print(response.text)
```

### 邮件生成

```python
response = model.generate_content("""
写一封商务邮件：
- 收件人：项目组全体成员
- 主题：项目进度更新
- 内容：本周完成了核心功能开发，下周进行测试
- 语气：专业、简洁
""")
print(response.text)
```

---

## 数据处理

### JSON 解析

```python
text = """
姓名：张三
年龄：28
职位：工程师
技能：Python, JavaScript, SQL
"""

response = model.generate_content(f"""
将以下信息转换为 JSON 格式：
{text}

输出格式：
{{
    "name": "",
    "age": 0,
    "position": "",
    "skills": []
}}
""")
print(response.text)
```

### 数据分析

```python
data = """
产品A: 销量1000, 利润5000
产品B: 销量800, 利润4800
产品C: 销量1500, 利润6000
"""

response = model.generate_content(f"""
分析以下销售数据：
{data}

请提供：
1. 销量排名
2. 利润率分析
3. 改进建议
""")
print(response.text)
```

---

## 翻译

### 多语言翻译

```python
text = "人工智能正在改变我们的生活方式"

response = model.generate_content(f"""
将以下文本翻译成：
1. 英语
2. 日语
3. 韩语

原文：{text}
""")
print(response.text)
```

---

## 错误处理示例

```python
from google.api_core.exceptions import ResourceExhausted, GoogleAPIError
import time

def generate_with_retry(model, prompt, max_retries=3):
    """带重试的生成函数"""
    for attempt in range(max_retries):
        try:
            return model.generate_content(prompt)
        except ResourceExhausted:
            if attempt < max_retries - 1:
                wait_time = (attempt + 1) * 5
                print(f"配额耗尽，{wait_time}秒后重试...")
                time.sleep(wait_time)
            else:
                return "错误：配额已用尽，请稍后再试"
        except GoogleAPIError as e:
            return f"API错误：{e}"
        except Exception as e:
            return f"未知错误：{e}"

# 使用
response = generate_with_retry(model, "你好")
print(response)
```

---

## 相关链接

- [[API快速开始]]
- [[Python-SDK]]
- [[认证与配额]]
- [[0 Inbox/_processed/AI-ML/Gemini/README|Gemini 知识库导航]]

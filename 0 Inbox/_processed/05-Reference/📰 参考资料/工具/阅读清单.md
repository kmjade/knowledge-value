---
recent_limit: "30"
---

### 有`$= dv.pages('"02-Reading/mdnotes" AND #unread').where(p=>p.file.name[0]=='@').file.length`篇未读文獻

```dataview
TABLE file.cday AS Date
FROM "02-Reading/mdnotes" AND #unread
WHERE file.name != "Mdnotes Default Template"
SORT file.ctime desc
```

# 顯示


### 近期已阅

**评分维度說明**

- 相關性：指文章与当前研究课题或者拟开展的课题的相關程度
# 分析
- 創新性：指文章的研究思路是否真正别出心裁让人有所启发

# 顯示

```dataview
TABLE file.tags AS 標籤, rating AS 评分, comment AS 初步印象
FROM "02-Reading/mdnotes"
WHERE file.name[0] = "@"
WHERE file.tags[0] != "#unread"
WHERE comment > 0
SORT file.mtime desc
LIMIT this.recent_limit
```



# 分享

```dataview
TABLE file.tags AS Tags, comment AS Comment
FROM "02-Reading/mdnotes"
WHERE share
SORT file.cday desc
```


### Top30被引排行榜

# 顯示

```dataview
TABLE file.tags AS Tags, comment AS Comment, length(file.inlinks) AS cited
FROM "02-Reading/mdnotes"
SORT length(file.inlinks) desc
LIMIT 30
```